export type PublicUser = {
    name?: string;
    email: string;
    avatar?: string;
};

export type PublicConfig = {
    domain: string;
    mailDomain: string;
    orgName: string;
    orgId: string;
};
