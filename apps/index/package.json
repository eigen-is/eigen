{
  "name": "@apps/index",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun vite build",
    "prebuild": "bun run scripts/build-content.ts && bun run scripts/build-licenses.ts && bun run scripts/build-changelog.ts",
    "postbuild": "bun run scripts/prerender.tsx && bun run scripts/build-search-index.ts",
    "typecheck": "bun run scripts/build-content.ts && tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@tanstack/react-hotkeys": "^0.1.3",
    "@workspace/lib": "workspace:*",
    "@workspace/ui": "workspace:*",
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.3.1",
    "markdown-it-anchor": "^9.2.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.2.0",
    "pagefind": "^1.5.2"
  }
}
