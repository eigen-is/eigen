# Roadmap

Prioritised backlog of open work. Each item was checked against the codebase on **2026-06-25**;
the P0/P1 proposals were deep-reviewed and re-verified against code on **2026-07-06**. Done items
are pruned on completion (last prune 2026-07-16 — git history keeps them). Status is
what exists today, not what the proposal hoped for. The detailed designs live in the
`docs/proposals/PROPOSAL_*.md` files referenced per row.

The organising theme is **the road to a trustworthy 1.0**: Eigen runs in production but is honest
about being pre-1.0, so the data-integrity work comes first. A grant application (NLnet Restack,
call opens 2026-09-03, deadline 2026-11-03) funds that push; the funding and launch tactics themselves
are tracked separately in `docs/superpowers/strategy/` (local-only) and are not duplicated here.

**Legend** — Effort: S / M / L / XL (rough T-shirt size). Frozen-format: Eigen is live, so any
change to a persisted format (Yjs roots, DB schema, drive MIME values) needs a migration and a
deliberate decision — flagged per row.

---

## P0 — Data-trust foundation for 1.0

The product's stated core weakness ("I would not yet trust it with data you cannot afford to
lose") and the work the grant funds. Highest strategic value.

| Item | Status in code | Effort | Frozen-format | Notes |
|---|---|---|---|---|
| **CRDT format migration** ([proposal](proposals/PROPOSAL_CRDT_MIGRATION.md)) | Primitive **already exists** — `restoreYjsDoc` (`packages/lib/src/core/collab/yjs-utils.ts`) does single-transaction live-state replacement, with snapshot-before-restore in `apps/api/src/lib/versioning/`. The system on top is net-new. Proposal written + reviewed 2026-07-05. | L+ | **Yes** — touches the document container format; design migration + snapshot rollback up front | Flagship. Net-new: version stamps per container (`doc_format` row in the container's `data.db`), a per-type migration registry, lazy-migrate-on-open + dormant-doc sweep, and a `?fmt=` sync handshake so stale clients can't write old-format data. |
| **Data integrity + verified backups** ([proposal](proposals/PROPOSAL_DATA_INTEGRITY.md)) | Seam F shipped 2026-07-13 (corrupt-snapshot restore now fails 422 + regression test); the rest not started. Proposal status updated 2026-08-04. Backup program phasing agreed 2026-08-20 — phase 1 (admin Users page, the UI surface for per-user backups) shipped 2026-08-21. | M | Yes, low — additive `paths.integrityCheckedAt` column (sweep cursor) | Cost-tiered checks at every write seam, scheduled corruption sweep (incl. the permanent orphaned-container scan), semantic backup verification (`verifySnapshotDb`), and a made-safe `scripts/backup.sh` (today it tars live WAL DBs — torn captures). **Agreed phasing:** ① admin Users refresh (done) → ② per-user backup/restore: self-contained per-home archive (manifest + `VACUUM INTO` DB snapshots + file tree + auth rows, S3 objects materialized), server-side artifacts + download, verify pass (SQLite `quick_check` + semantic collab-doc decode), restore-in-place with replace semantics, UI in the user detail pane (spec: [PROPOSAL_BACKUP_RESTORE.md](proposals/PROPOSAL_BACKUP_RESTORE.md), written 2026-08-21, covers ②–④) → ③ whole-server backup: loop over all homes + auth/config DBs, scheduling + retention, admin Settings section — retires the unsafe live-tar `backup.sh`; **must not slip far behind ②** since eigen.is is live on torn-capture backups → ④ migrate: restore an archive on another server (identity/share re-linking) + live mount-level storage-backend migration (local↔S3) reusing copy/upload-queue machinery. |
| **E2E multi-user collab test suite** ([proposal](proposals/PROPOSAL_E2E_COLLAB_TESTING.md)) | Not started. Proposal written + reviewed 2026-07-05. | M | No | Playwright, multiple concurrent clients, CI. Must be reliable from the start — flaky suites get abandoned. Phase 1: four docs/Tiptap tests on one shared convergence primitive; retries=0. |

## P1 — High value-per-effort, start next

| Item | Status in code | Effort | Frozen-format | Notes |
|---|---|---|---|---|
| **Calendar import + subscriptions** ([proposal](proposals/PROPOSAL_CALENDAR_IMPORT.md)) | 0%, on a complete `parseIcs` foundation. Proposal rewritten 2026-07-06: feed refresh must NOT reuse the public event mutations (they propagate cancellations to attendees) — one bulk `applyFeedSnapshot` transaction with `(uid, recurrenceDate)` identity; refresh on access (web range GET + CalDAV reads), no timers. | M | Yes, **low** — additive nullable `subscription` column on `calendars` | Best *new* feature: `.ics` import + read-only feed subscriptions. Workspace parity. SSRF decided: private feed URLs allowed by default (`calendar.allowPrivateFeedUrls`, MinIO-on-LAN precedent) behind always-on guards (scheme, userinfo, link-local/metadata block, per-redirect revalidation, size cap); hosted multi-tenant sets it false. |
| **Calendar events as `.ics` files** ([proposal](proposals/PROPOSAL_CALENDAR_ICS_FILES.md)) | 0%. Proposal written 2026-09-03. Calendar is the last domain whose truth is SQLite-only (mail = Maildir + `mail.db`, contacts = `cards/*.vcf` + `contacts.db`); `events.icsBlob` is vestigial. | L | **No BC by decision (2026-09-03)** — a `calendar.db` version bump drops existing calendar data; clients full-resync | One `.ics` per UID under `eigen.calendar/<calendarId>/`, `calendar.db` becomes a rebuildable index + calendar-level metadata, CalDAV GET serves stored bytes verbatim with a hash ETag, `X-EIGEN-*` props re-stamped on PUT carry the event id and the invitation links, `Calendar` goes async. Phase 0 extracts the file+index core out of `contacts/` (third copy → shared). Lands before, and simplifies, calendar import + subscriptions. |
| **Sheets sync: make the op log honest** ([proposal](proposals/PROPOSAL_SHEETS_YJS_WORKBOOK.md)) | 0%. Researched 2026-09-03 in answer to "can sheets be a real Yjs document with `Y.UndoManager`?". Sheets keeps one JSON snapshot string plus an op log in Yjs and its own immer undo stack; every other editor keeps the document in Yjs. Findings: cells have no stable identity, so a `"r_c"`-keyed `Y.Map` is a trap (row inserts re-key the whole sheet, concurrent inserts corrupt it); the real Yjs shape needs stable row/column ids and id-based formula references, an engine-level change (L–XL, ~200 MB server RAM per open 340k-cell workbook, ~10 B permanent dead weight per map write). `Y.UndoManager` is in-memory and per-tab like the engine's stack, so undo alone does not justify it. | S–M each | No (sheets has no BC requirement, Reinder 2026-09-03) | Four in-place fixes, independent, ~1 week together: (1) agreed order for same-key conflicts (Design A in [PROPOSAL_SHEETS_YJS_CONFIG.md](proposals/PROPOSAL_SHEETS_YJS_CONFIG.md)); (2) transform concurrent row/column inserts against each other; (3) server-side compaction of the op log into the snapshot, so no browser tab has to flush and the grid stops remounting on a peer's snapshot; (4) shift the undo stack on incoming inserts + never clobber a peer's later edit. The stable-id Yjs build is deferred until the engine needs ids for another reason. |
| **SSO / enterprise login** ([proposal](proposals/PROPOSAL_SSO.md)) | 0%. Foundation stronger than assumed — the `user.create.after` hook auto-provisions org + reconciles shares, and Home/maildir/quota bootstrap is already lazy + idempotent on first `getHome()` (no waitlist coupling — no provisioning work needed); app-password protocol-auth already works for passwordless users. `@better-auth/sso` not yet installed. Proposal rewritten 2026-07-06: v1 OIDC-only, SAML deferred. | M | Yes — additive `@better-auth/sso` provider tables in `users3.db` (auth-schema migration) | OIDC via better-auth's `sso` plugin (SAML + setup-wizard step deferred to phase 2), providers registered at runtime → admin page. **Demand-gated:** pull forward when a concrete org needs it. Key work: disable the plugin's own org-provisioning (double-`addMember` hazard against the existing hook), gate its built-in `/sso/register` endpoint, account-linking policy; SSO users use app passwords for IMAP/CalDAV/WebDAV. |
| **Durable home-relay outbox (queue for cross-home delivery)** ([proposal](proposals/PROPOSAL_HOME_RELAY_OUTBOX.md)) | ACL fan-out is async since 2026-07-04 (bounded-concurrent, per-path FIFO, drain-on-shutdown — `acl-propagation.ts`) and the ACL route takes deltas merged server-side. Remaining: the durable outbox itself. Proposal written + reviewed 2026-07-05 (server-level `outbox.db`, per-lane FIFO + coalescing with conditional ack, global K=8 delivery budget). | M | Yes, low — new server-level `outbox.db` (additive) | Persisted delivery rows + per-recipient FIFO + retry/backoff + replay-on-boot, following the existing `UploadQueue`/`ContentReindexQueue` pattern (third instance of "durable rows + self-scheduled drain"). Closes the crash-loses-in-flight-deliveries window and replaces silent per-recipient skips with retry. Also the stated sharding trajectory — in a multi-server deployment the outbox *is* the message transport (see SCALABILITY.md). **Build it in the relay, not per seam** — the 2026-07-04 seam audit found the same sync-fan-out pattern in: (1) `propagateCalendarShare` (`calendar/share-propagation.ts`, awaited on the share PUT — direct sibling of the fixed ACL bug, N sequential cold home opens); (2) chat/comment notifications (`chat.ts` mention + activity loops, awaited inside `postMessage` — cold opens for every previous participant add latency to every message send; coalesce-by-tag makes ordering a non-issue); (3) `FileHistory.notifyWatchers` (`drive/history.ts`, concurrent but **unbounded** `Promise.all` awaited on every drive mutation — the fd-burst class); (4) signup reconciliation (`share/reconciliation.ts`, sequential in the `user.create` hook — rare, note only). Calendar invitations (`invite-propagation.ts`) are already fire-and-forget but sequential and carry a real create-vs-update ordering hazard (`sequence` can apply out of order) — they need the outbox's per-target FIFO, not just parallelism. One global concurrency budget in the relay/outbox subsumes all per-seam semaphores and closes the fd-burst class everywhere at once. |
| **File-descriptor budget: graceful exhaustion** ([proposal](proposals/PROPOSAL_FD_BUDGET.md)) | Compose pins `ulimits: nofile` since 2026-07-04. Discovered during the slow-share investigation: a 26-home fan-out **crashed with `SQLITE_IOERR_VNODE` under macOS's default `ulimit -n 256`**, silently skipping recipients. Measured warm ceiling ≈ 30 fds per open Home (6 eager WAL databases × 3 files + 12 maildir `fs.watch` handles; more per extra mount / open doc container). Proposal written + reviewed 2026-07-05. | S | No | Phase 1: document the `LimitNOFILE` requirement for bare-metal deploys + startup check — read the soft limit via `process.report` userLimits, log a loud warning below a derived threshold. Phase 2 (optional, gated on need): LRU cap on resident Homes via the existing `homeFactories`/`touch()` seam (eviction predicate incl. open collab connections) + `ManagedDatabase.open` retry-once-after-eviction. |
| **iMIP occurrence-notification product call** | The audit deep-dive remainders shipped 2026-07-13 (upload-queue orphaned-PUT repair; CalDAV #C–#G; outbound occurrence-REPLY scoping); the deep-dive docs are removed — see git history. What's left here is a decision, not code. | XS | No | Occurrence-level inbound iMIP changes broadcast SSE but persist no tray notification, while master-level changes do — awaiting Reinder's call on whether instance-level changes deserve a persisted notification. |

## P2 — Finish what's already started

| Item | Status in code | Effort | Frozen-format | Notes |
|---|---|---|---|---|
| **Vector drawing app: embedding (phases 5–6)** ([proposal](proposals/PROPOSAL_VECTOR.md), [CANVAS.md](CANVAS.md)) | Phases 0–4 + the arrows program merged 2026-08-31 @1828d879c (freehand/line/eraser; elbow arrows with excalidraw-parity routing, pinnable segments, side-anchored docking; draggable snapping focus points; SVG preview/export with serve-time media+font inlining; slides/stickies double-click create). The vector-polish round (2026-09-01) then shipped freedraw **pen pressure** (perfect-freehand widths, legacy strokes pixel-identical), **elbow first/last-segment pinning** (jog model), **curve-exact bound docking** for curved arrows, and a **foreign-visible inlined-SVG clipboard flavour** on menu Copy/Cut (an `<img src="data:svg…">` with base64 images, so a drawing pastes as an image into a plain contenteditable), and **touch/stylus tuning** (pen-latch palm rejection, two-finger pan + pinch zoom, coarse-pointer hit slop, double-tap text editing). Cross-app SVG image paste by name refs shipped earlier (the eigen name-ref SVG is unchanged; the polish round only added the foreign-visible flavour on top). | M | No (`.eigenvector` shape may still change — not BC-frozen) | Remaining: drawings embedded in docs/sheets as nested `drawings/` sub-resources (phases 5–6; the slides half is superseded by the canvas-engine row below); grouping deferred to a future round unified with slides' object model, alongside the last-used style memory row. Ruled out forever 2026-09-01: minimap, shape library, PNG export. |
| **Slides on the vector engine** ([proposal](proposals/PROPOSAL_CANVAS_ENGINE.md), [CANVAS.md](CANVAS.md)) | 0%. Proposal written 2026-09-03 after a four-way inventory of both editors and the server pipelines. Vector and slides share the transform chrome, the snap/marquee/arrange math, the panel rows, the fonts, the collab lifecycle and the comment cards, but not the element model or the host loop: ~1,200 lines exist twice and two behaviours already disagree (additive-select modifier, z-order stepping). Slides has no shapes or arrows; a pasted vector selection flattens to one image. | XL | **No BC** for `.eigenslides` and `.eigenvector` (Reinder 2026-09-03); MIME/extension values stay frozen | Slides becomes a deck of pinned 1920×1080 frames on the vector element model: a `frames` root + `frameId`, a `richtext` element (TipTap HTML as a plain div; the live canvas becomes an HTML base with one `<svg>` per element, edited in place), per-element `commentCardIds`, one `CanvasEditor` with a `viewport` mode, one export compositor. Five phases (spike, host, model, slides shell, parity), 8–13 wk; phase 1 pays on its own (vector gets per-element comments + ⌘F). Replaces the slides half of the embedding row above; docs/sheets embeds unchanged. |
| **Partial-failure semantics of `useCopyToMediaFolder`** | **Done** (vector-polish, 2026-09-01) — the hook now partitions its `Promise.allSettled` results: the succeeded `DrivePath`s resolve to the caller (which places them), a partial failure toasts the dropped count in-hook (no throw, so the successes aren't discarded and don't strand orphaned files in `media/`), and only a total failure throws so `onMutationError` toasts and card-attachments' Save-abort stays intact. Fixed once in the hook, not per call site. | S | No | Shipped; the earlier symptom (callers discarding successful copies on any failure, orphaned files, retry duplicates) is closed. |
| **Editor-toolbar Insert-menu scaffold (fourth copy)** | **Done** (vector-polish, 2026-09-01) — the shared `ToolbarMenu` (`packages/ui/src/components/layout/toolbar/toolbar-menu.tsx`) now owns the ghost-Button trigger + `DropdownMenuContent align="start"` shell beside `FileMenu`/`EditMenu`; slides/docs/stickies/vector pass their own item lists. | S | No | The compact-breakpoint **fold stayed caller-side**, not in the wrapper (it differs per app, so the shell stays policy-free — the original "compact fold policy in the wrapper" plan was dropped). **Follow-up (third-copy leftovers, noted by review):** adopt `ToolbarMenu` for drive's markdown-toolbar menu and `FileMenu`, which still hand-roll the same trigger+content shell. |
| **Paste-ladder extraction (cross-app clipboard dispatch)** | **Done** (vector-polish, 2026-09-01) — `classifyPaste` (`packages/lib/src/core/clipboard/classify.ts`) does one sync pass over the `DataTransfer` and returns every present flavour once (`eigen`, `svg` + backing items, `imageFiles`, `files`, `html`, `text`), composing the existing readers so the payload is parsed once. Each app keeps only its own short ladder + per-kind handlers; the `internalMarkerText` option owns sheets' same-tab marker-skip. See [CLIPBOARD.md](CLIPBOARD.md) § Classifying a paste. | S–M | No | Shipped; rung ORDER stays per-app by design (the classifier imposes none), and rich-HTML arbitration (`hasRichHtmlBeyondMarker`) stays caller-side. |
| **Search: calendar events** ([SEARCH.md](SEARCH.md)) | Mail + drive search (names and body content) shipped — as-built in [SEARCH.md](SEARCH.md); calendar is the last unsearchable domain. | S | Yes, low — additive FTS table, following the inline-FTS pattern mail.db uses | First item of SEARCH.md § Remaining. |
| **File History: email digests + version-history unification** ([FILE-HISTORY.md](FILE-HISTORY.md)) | Core live: `FileHistory`, Recent Activity, Watch, Watched view, in-doc Activity panel + live SSE refresh — as-built in [FILE-HISTORY.md](FILE-HISTORY.md). | M | Yes — email channel needs a `notifications.db` migration | Remaining: email/digest channel, unifying the in-doc Activity panel with Version History, and richer sheet/slide/doc semantic events. |
| **Curve-exact arrow binding (vector)** | **Done** (vector-polish, 2026-09-01) — a bound curved (round, ≥3-point) arrow now docks on where its rendered Catmull-Rom shaft crosses the shape outline (`curveOutlineDock` / `arrowCurveBeziers`, `packages/lib/src/vector/geometry.ts`), not the straight chord, so the drawn curve meets the outline exactly at the head. The curve is golden-locked to roughjs at roughness 0, and the dock is endpoint-independent so it stays a strict fixed point under `followBindings`. See [CANVAS.md](CANVAS.md) § arrow bindings. | S–M | No | Shipped; straight/2-point arrows keep the Excalidraw chord dock. |
| **Last-used style memory (canvas apps)** | Not started — deliberately deferred 2026-08-31 during the vector arrows round (decision recorded in the U-FID audit: no Eigen app remembers last-used styles; Excalidraw does). | S–M | No | If built: ONE shared store consumed by vector and slides (and stickies where sensible), never a vector-only quirk. Decide when canvas usage feedback exists. |
| **Copy-Paste Phase 0** ([proposal](proposals/PROPOSAL_COPY_PASTE.md)) | v1 clipboard live; the async path (`writeEigenClipboardAsync`) omits the custom MIME. | S | No (clipboard is transient) | Tiny fix to make Slides button-copy lossless. Full ECP v2 protocol is P3. |
| **Help center: article gaps** | Build + 120 articles shipped — as-built in [HELP-CENTER.md](HELP-CENTER.md) (shared AppShell instead of the spec'd SupportSidebar was deliberate; Pagefind kept). The content campaign is finished; its plan doc is removed. | S | No | Unwritten articles: keyboard shortcuts; docs "import from another doc" + real-time-collab guide; mail "respond to a calendar invite"; admin self-hosting guide (decide where it lives). Blocked-on-feature articles (sheets charts/pivots, slides themes/speaker notes, calendar subscribe, contacts vCard import/export/merge, chat formatting, mail filters/out-of-office/schedule-send) wait for the features. Contextual `getHelpUrl(section, slug)` deep links were never built. |
| **Frontend unit-test harness** | Nothing exists for the frontend — `bun run test` runs every workspace's tests (api integration + sheet + lib + index), none covering FE hooks or components; the API suite alone is `bun run test:api`. The FE's hooks and state machines (`useLongPress`, the drill-in menu, `useDocumentPanels`) are covered by browser rounds and review, not by tests. Raised as P2 by an external reviewer of the mobile pass; roadmapped 2026-08-03. | S–M | No | `bun test` + happy-dom + `@testing-library/react`, scoped to `packages/ui` hooks and state machines first. Pin behaviour (timer thresholds, page stacks, one-panel-at-a-time), not rendering details. |
| **Mobile: next round** ([MOBILE.md](MOBILE.md)) | Phases 1–5 merged 2026-08-04 (one PanelColumn on every viewport, docs panels from 768, slides view-only). MOBILE.md tracks what's left. | S–M | No | Sheet tab strip below 640, tap-target pass, drive multi-select Select mode, stickies mobile activity pane, real-device checklist — accepted drifts recorded in the same file. |
| **Command palette v2** ([proposal](proposals/PROPOSAL_COMMAND_PALETTE.md)) | v1 shipped (engine + 13 UI files); all six v2 items unbuilt. | M | No | Sub-actions, `event:`/`chat:` scopes, recents, paste/drop into the palette. Nothing blocked on it. |
| **Comment threads: per-user unread state** | Scaffolding exists and is dead: every `.eigenchat` has a `read_state` table (`apps/api/src/lib/chat/schema.ts`) written only by `Chat.markRead` via `POST /chat/:ownerId/:mountId/:chatId/read` — no frontend calls that route, nothing ever reads the table back, and the shared `ChatReadState` type has zero consumers. Raised 2026-08-27 while making the comment badge personal (it now counts open threads assigned to you; "unread" is the signal it cannot give). | S–M | Yes, low — the table already exists; exposing it means widening the comments list response | Today the only per-user signal is notifications: `comment-reply` / `mention-comment` carry the thread in their tag (`comment-reply:{ownerId}:{mountId}:{containerPathId}:{chatName}`), so per-thread unread is derivable the way `useUnreadChatIds` derives per-chat unread — but coverage is partial by design (activity notifications go to previous participants + owner only, so a member who never replied has no signal). Two pieces of work: (1) **bug** — opening a comment card doesn't clear its notification, because `CommentThread` → `useChatRoom` passes the *comment chat's* pathId to `useAutoMarkChatRead` while comment tags carry the *container* pathId at index 3; (2) wake `read_state` up — write it when the card dialog opens, return it on the comments list so `CommentEntry` carries `unread`, then decide what the badge and the panel rows show (unread dot per row, and whether the toolbar badge becomes assigned-to-me + unread). Preferred over the notification derivation: it survives notification purging and covers people who were never notified. |
| **Team-removal collab read-revocation** | Not started — the top recorded follow-up of the 2026-07-03 audit-fix branch (ledger: `docs/superpowers/audit-fixes/progress.md`, local-only). The branch made read-revocation coherent for `updateACL` + `movePath`; removing a member from a *team* still doesn't revoke their live collab read access on docs in other homes. | S–M | No | Cross-home, so it needs the relay — bundle with the P1 durable home-relay outbox rather than building a bespoke fan-out. |

## P3 — Defer

Large net-new builds or low value-per-effort today.

| Item | Status | Effort | Why defer |
|---|---|---|---|
| **Home Recents** ([proposal](proposals/PROPOSAL_HOME_RECENTS.md)) | 0% | M | Polish for autosuggest/palette; nothing is blocked on it. Its old blocker (File-History actor threading) has shipped; needs a new `recents.db` migration. |
| **Copy-Paste ECP v2** ([proposal](proposals/PROPOSAL_COPY_PASTE.md)) | 0% | L | Cross-app converters + BroadcastChannel bus + cut coordination. Current text/image cases already work. |
| **Graphs / charts** ([proposal](proposals/PROPOSAL_GRAPHS.md)) | 0% | XL | Table-stakes eventually, but net-new across sheets/docs/slides with new frozen Yjs formats. Remove the dead fortune-sheet chart scaffolding first. |
| **Scripting platform** ([proposal](proposals/PROPOSAL_SCRIPTING_PLATFORM.md)) | 0% | L | Foundations exist (document layer, ACLs, SSE, CodeMirror), but "Deno isn't a true sandbox" caveat. Post-1.0. |
| **AI integration** ([proposal](proposals/PROPOSAL_AI.md)) | 0% | XL | Needs a GPU sidecar the host lacks; its flagship semantic search is superseded by the FTS5 plan. Keep as grant narrative, not a build. |
| **Stalwart mail backend** ([proposal](proposals/PROPOSAL_STALWART_MAIL.md)) | 0% | XL | The proposal's own decision section says **don't build it** until a user asks for JMAP. The `MailStore` seam it would plug into exists since 2026-07-03. |
| **Async `MailStore.search`** | Interface landed 2026-07-03; `size()` is already async, only `search()` is sync | S | Only needed once a second (remote) mail backend exists — widen the type together with that backend. |
| **Transform queue → preparation closures** | Accepted drift, recorded in [DOCUMENT-TRANSFORMS.md](DOCUMENT-TRANSFORMS.md) § History (2026-08-04) | M | Queued transform jobs retain their full payloads (captured Yjs blobs, upload copies, export media) instead of the original proposal's "small job descriptors or preparation closures". Bounded in practice (~5 foreground exports + 8 background admit; payloads capped by upload quota / doc size), and admission is now checked before capture and media prep. Refactor to closures (capture/prep when the job starts) only if queued-payload memory shows up in production — it reworks the reviewed runner seam (prepare-error propagation, abort-during-prepare, the whole runner suite). |
| **Dangling card-chat cleanup + same-name card retry** | 0% | S | The two leftovers of the create work ([STORAGE.md § Creating a container](STORAGE.md#creating-a-container)), both scoped out because atomic create plus create reconcile stop the inflow. (1) A one-off `scripts/` pass that loads each board container's `data.db`, collects the `chatName` values from its `tasks`/`comments` maps, lists the `chat/` subfolder and reports chat containers nothing references, older than a 24 h grace so it can never race a create in flight; report-only by default, trashing only on an explicit flag. (2) A retried card create still mints a fresh `fileName` per click, so two clicks that both reach the server leave one chat behind; reusing the name across clicks would need the card's name to be picked once per intent, not per attempt. Worth building only if litter reappears. |

## Cheap wins (broken-window fixes)

| Item | Effort | Notes |
|---|---|---|
| Chat/comment `authorId` → email-only (verify) | S | Frozen-format (per-container `data.db` migration if pursued). Chat `messages` rows store both `authorId` (user id, NOT NULL) and `authorEmail`; display resolves from **email** (`chat-message-list.tsx:453-455`), and `authorId` is used only for the self-message check (`:223`). Comments are already email-keyed. **Verify** whether `authorId` can be dropped in favour of `authorEmail` (self-check compares current-user email instead) — it is the last user-id embedded in container bytes, and dropping it makes chat/comment fixtures fully id-free. Surfaced by the 2026-07-14 demo-mode ID audit. |
| Maildir delivery durability (fsync + crash-safe rename protocol) | S–M | The CardDAV phase-1 wave (2026-08-16) set a new durability standard: `LocalFilesystem.writeAtomic` fsyncs the temp file **and** its parent directory, and contacts writes are journaled + crash-recovered on init. Maildir delivery (`maildir-store.ts`) still fsyncs nothing and its tmp→new move crosses directories, so a power loss can drop an acknowledged delivery — mail is now measurably less crash-durable than contacts. Needs its own protocol (fsync the temp file, rename, fsync **both** directories); the same-directory `writeAtomic` helper is NOT a drop-in — a naive swap was explicitly rejected in the CardDAV review as incorrect for cross-directory moves. Same class as the P0 data-integrity write-seam work; bundle there or do right after. |
| Contacts: a value edit drops its vCard params | S–M | Decided trade-off from the CardDAV branch (documented in [CONTACTS.md](CONTACTS.md) § Caveats): the merge diffs `EMAIL`/`TEL` by value, so retyping a work email deletes the old line and appends a bare one — `TYPE=WORK` and the grouped `X-ABLabel` don't carry, and the edited address reaches the phone unlabelled. Revisit with a pair-the-single-change heuristic: when an edit removes exactly one line and appends exactly one value, reuse the removed line's group + params. No frozen-format impact. |
| Mail-size quota: live byte counter over the TTL cache | S–M | `enforcement.ts` memoizes the O(N) maildir walk for 15 s and invalidates on delete — pragmatic, but the elegant fix is a live byte counter in `MaildirStore` mirroring `Contacts.size()`'s in-memory counters, killing the walk and both staleness directions. Bundle with the maildir-durability row above (same file, same review lineage). |
| Contacts edit form: external sync discards unsaved edits (product call) | XS–S | The edit route remounts the form when the contact's etag changes (`key={contact.etag}`, `_auth.edit.$filterType.$filterId.tsx`), so a CardDAV sync landing mid-edit silently resets the form to the synced card. Accepted during the CardDAV final review; the call is between keeping it (form always reflects fresh data) and an isDirty guard + conflict prompt before remounting. |
| Contacts: web-UI vCard import/export | S | The CardDAV storage redesign (contacts ARE plain `.vcf` files under `eigen.contacts/cards/`, 2026-08) makes this nearly free — export is serving the existing files (a single card, or a zip / concatenated book), import is the already-device-tested PUT/parse path fed from an upload instead of over HTTP. Table-stakes for onboarding from Google/Apple exports, and it unblocks the help-center's "contacts vCard import/export" article (listed in the P2 Help center gaps). Added 2026-08-18. |
| DAV consistency wave deferred minors (2026-08-18 ledger) | S | The 2026-08-19 DAV finish pass closed items (1)–(3) of this ledger plus the MKCALENDAR and PROPFIND-prop-list rows (multiget 404 rows for malformed hrefs, shared 207 wrapper, malformed-escape test — see git history). Remaining, all triaged fine-to-defer and unlikely to ever justify a dedicated session: (4) `REPORT_BODY_MAX_BYTES` caps three verbs (REPORT/MKCALENDAR/PROPPATCH) under a REPORT-specific name, and the cap constants are deliberately spelled in both `report.ts` siblings — `lib/dav/propfind.ts` now exists as a shared constants home if centralizing on next touch. (5) `calendarCollectionProps` advertises no `max-resource-size` while CardDAV's book props advertise the byte value (advertising is optional, RFC 4791 §5.2.5). (6) Bounds tests assert over-cap rejection only, never that an exactly-at-cap body passes. (7) WebDAV PROPFIND (`lib/webdav/propfind.ts`) still answers a fixed prop set regardless of the requested props — the RFC 4918 §9.1 prop-list handling shipped 2026-08-19 covers the CalDAV/CardDAV surfaces only; deliberate scope cut (WebDAV is litmus-tested against Finder/Windows Explorer), reuse `lib/dav/propfind.ts` if a client ever cares. |
| CalDAV: no calendar DELETE | S | The router's DELETE requires a resource uri, so a calendar created from a client via MKCALENDAR (shipped 2026-08-19) cannot be removed by that client — DELETE on the collection URL is 400 and the calendar can only be deleted from the web UI. Pre-existing gap, newly reachable in a normal flow now that clients can create calendars. Honor DELETE on the calendar URL, mirroring the web route's guards (the default calendar cannot be deleted). Surfaced by the 2026-08-19 DAV-branch review. |
| Load-sensitive test flakes: margin fix owed | S | Two tests fail only under full-suite load and pass in isolation — `upload-queue-chaos.test.ts` "control: timed-out PUT" (~50 ms margins, known 2026-08-16) and `contacts-reconcile.test.ts` "owner-contact seeding" (`no such table: member` from `getOrgOwner` — shared-auth-DB ordering, first seen 2026-08-17). Re-run in isolation before treating any as a regression; the fix is widening the margins / making the assertions load-tolerant. |
| rspamd sidecar + spam learn-loop ([proposal](proposals/PROPOSAL_RSPAMD.md)) | 1–2 d | The real fix for the spam/DMARC pain that the Stalwart proposal exists to solve. rspamd+Redis as a Postfix milter (edge scoring, SPF/DKIM/DMARC); `mailboxDeliver` routes flagged mail to Junk; the existing "Report Spam" button trains Bayes via `/learnspam`·`/learnham` in `messageMove`. Fail-open, no frozen-format impact. |
| Per-SASL-user outbound rate limit | S–M | The only fix that would have bounded the 17k run, since the login itself was legitimate. Postfix has no per-SASL-user message limit, so it needs a `check_policy_service`: either a small policy endpoint in the API (raw TCP, new surface) or a fourth mail container such as postfwd. Deferred out of the 2026-08-31 hardening branch as materially more work than the three fixes there. |
| fail2ban filter drift: update.sh doesn't refresh the `/etc` copies | XS | The live fail2ban config is a copy — the install (SETUP-GUIDE § Mail abuse hardening) `cp`s the `filter.d`/`jail.d` files into `/etc/fail2ban`, so a filter improvement in the repo never reaches installed hosts. Fix: update.sh re-copies the two `filter.d` files (ours, safe to overwrite) before its `fail2ban-client reload` when the jails are installed — but never `jail.d`, because the README tells admins to tune `ignoreip`/thresholds there. Added 2026-09-01. |
| Audit-branch deferred minors (2026-07-03 ledger) | S | Recorded, not fixed, all verified still present 2026-07-11: (1) `TeamHome.updateMount` persists-before-push — a mid-push `addMount` init failure on a storage re-point leaves settings enabled with no live mount until eviction (rare, self-heals on reload; add catch-and-revert). (2) HTML (non-PDF) export keeps remote `url()` in inline CSS — client-side beacon; needs a CSS `url()` stripper. (3) O(N·depth) descendant re-walk on permanent delete of large trees (s3/local-key perf). (4) Large-numeric `@page` values in PDF export — weasyprint cost blow-up. |
| Audit P3 duplication folding | S–M | The frontend-quality-review pass (2026-08-13) folded drive's 4 list handlers onto `useDriveListRoute` and the per-app `_auth.tsx` variants onto `createAuthRouteOptions`, and split `use-drive.ts` along its seams. Still open: the copy-mutation quartet (4 near-identical call-sites, now in `writes.ts`) and the parallel per-domain action lists — the same action reimplemented across desktop toolbar, compact/kebab, right-click, command palette, and mobile menus, drifted in permission, label, keyboard, and visibility: extract a small per-domain action catalog domain-by-domain as each domain's drift is fixed, following Drive's shared item-menu/create-menu pattern, not a generic menu framework. |
| Frontend-quality-review deferred minors (2026-08-13 ledger) | S | The program's review doc was removed after everything shipped (git history keeps it, incl. the per-finding tick-off and five refuted claims). Recorded, not fixed: (1) invite read path — `messageGet` re-parses every `text/calendar` attachment with ical.js on every open (sender-controlled N×size amplification, LOW: linear, no recurrence expansion, malformed input contained); persist the summary at delivery or first-part short-circuit + size guard — needs a product call on multi-invite messages. (2) `export/sheets/render.ts` spells the sans stack two ways in one file (hardcoded vs `FONT_STACK_SANS`); unifying shifts golden-pinned export bytes, wait for a golden-regenerating touch. (3) ~20 deep imports of primitives that gained barrels late (`DocSearchProvider`, `SidebarPrimaryButton`, `useDocumentExport`, …) — sweep onto the barrel on touch. (4) `TEAM/SPACE_SETTINGS_UPDATED` SSE members exist but nothing emits them (dead handlers were deleted) — either emit on the settings routes for cross-device freshness or drop the members. (5) eigendoc list/shared views hand-roll `useDriveListRoute`'s contract — unifying adds preview-on-select + quick-look there, a product call. (6) Drive's detail-kebab labels doc export "Download" (intentional, reads ambiguously next to true downloads). (7) Chat rename pencil and message Edit share `aria-label="Edit"`. (8) Avatar uploads lost byte-level progress when the workflow moved onto a lib fetch mutation (accepted; restoring means moving the XHR transport). (9) `apps/api/package.json` exports `./src/index.js` for a `.ts` file (pre-existing, type-erased importers only). |
| Mail-attachment-access deferred minors (2026-08-16 ledger) | S | The branch's review doc was removed after all 17 merged findings + 4 post-batch findings shipped (git history keeps it). Recorded, not fixed: (1) `AppError` (`packages/lib/src/core/api-error.ts`) renders Elysia validation bodies as `[object Object] (422)` in production — product-wide, every schema rejection; pick a readable fallback. (2) `buildRecipientSummary` (`mailutils.ts`) doesn't flatten RFC 2822 groups for the list row / search text — a product call, since flattening `undisclosed-recipients:;` blanks the To column where the group name is the only label. (3) Attachments-only send (no subject, no body, one file) is blocked FE + BE — consistent, but as legitimate as the ref-only send now allowed. (4) `handleReplyEmail` passes the original `Reply-To` AddressObject through the `text` path, so a group there round-trips as parser text — left because `text` preserves quoting for names with commas. (5) The compose-side flow (`sendWithFreshDraft`, `ShareAndSendDialog`, the save lifecycle) has no test harness; three of the six must-fix items lived there. |
| Batch idempotency: duplicate-on-retry in client/loop batches | S–M | Client-driven and sequential-loop batches aren't retry-safe: a partial failure leaves the succeeded items persisted, and a retry from the (correctly retained) picker/dialog re-sends them, duplicating. Instances: drive copy (`useCopyFiles` sequential loop, `writes.ts`), drive trash (`useDeletePaths` partial), mail attachment save (`saveAttachmentsToDrive`). The 2026-08-04 code-review pass narrowed the window (validate-before-write on save-to-drive; retain-only-failed on permanent mail delete) but the general class wants a per-item partial-result contract or a server-owned batch op. Server-side sibling is the P1 home-relay-outbox fan-out class. |
| Long silent drive/WebDAV copies die client-side past ~200s | S–M | `POST …/path/:pathId/copy` and WebDAV COPY stream no response bytes and thread no abort signal; a >200s cross-mount/S3 container copy hits the server-wide `idleTimeout: 200` (`apps/api/src/index.ts`), the client sees an empty reply while the copy completes server-side, and a retry duplicates the tree. Same class the 2026-08-05 transform-route fix closed (`server.timeout(request, 0)` per-request) — apply the exemption or stream progress. |
| Docs print takes the page transform with it | S | Pre-existing, and reachable more often since phase 5 of the mobile pass: below ~1000px with a panel open the docs page is shifted and scaled, and print puts that transform on paper. Reset the transform in the print path. |
| Docs desktop comment panel overlays the page scrollbar | S | Pre-existing; sheets got the resize-to-panel-edge fix in phase 5, docs did not. Shrink or inset the docs scroll container when the panel is open. |
| Shared `useElementSize` / `useResizeObserver` primitive | S | Four hand-rolled callback-ref + `ResizeObserver` pairs with the same 0×0 skip: docs editor (×2), `drive-grid`, `drive-preview`. One `packages/ui` hook would carry the guard once. The sheet opts out — its resize path also reads `devicePixelRatio`. |
| Command palette console noise | XS | Radix `DialogTitle` a11y warning plus a `pagefind.js` 404 in the dev console. Pre-existing, in code no recent branch touched. Surfaced by the 2026-08-03 mobile round. |
| Sheets: formula bar stale after undo | S | Undo while the same cell stays selected leaves the old formula in the bar. Display lag only, the cell itself is correct. |
| Sheets: `key={snapshotVersion}` remounts the whole Workbook | S–M | Every remote snapshot throws away the mounted grid and rebuilds it. Waste class, not a bug today; revisit together with sheets collab work. |
| Thin frontend apps: comment density ~1%, zero tests | S–M | admin, calendar, drive, chat and contacts sit at roughly 1% comment lines and have no tests (the other apps are 4–9%). Thin apps need few comments, but these are also where the per-domain action lists have drifted (see the Audit P3 duplication row) — read them for duplication first, and make them the first target of the P2 frontend unit-test harness. Surfaced 2026-09-01. |
| Bun-native micro-touches (last open bits of the removed apps/api audit) | S | `createHash('md5')` → `Bun.CryptoHasher` at `calendar/mappers.ts` + the `s3-storage.ts` SigV4 probe; `Bun.spawn({ timeout, killSignal })` over the hand-rolled `setTimeout → kill` in `preview/weasyprint.ts` + `preview/video-thumbnail.ts` (native reaping, matching the exiftool timeout fix). Marginal — do on next touch of those files. |
| Shared drive watch list is N+1 | S | `SharedDrive.getWatches` (`apps/api/src/lib/drive/sharedDrive.ts`) calls `breadCrumb` once per watched path to re-check read access, so a user with many watches on a shared drive pays one ancestor walk per row on every list. Batch the ancestor lookup per mount, or cache the crumb per parent id within the call. Surfaced by the Tier 2 standards audits of `lib/drive` and `lib/home`. |
| `Home` subsystem getters are typed non-null but absent on team, guest and org homes | S–M | `home.contacts`, `home.mail`, `home.calendar`, `home.notifications` return non-null types via definite-assignment fields, yet `TeamHome` builds only drive + calendar, `GuestHome` drive + notifications and `OrgHome` nothing, so callers optional-chain through non-nullable getters (`calendar.ts` ×5, `mail-domain.ts`, `enforcement.ts`, `home-relay.ts`) and the `drive` getter hides a `!`. Honest fix: `X \| undefined` getters (or a `has*` flag per subsystem like `hasCalendar`) and a sweep of the consumers. Typing debt, not a runtime bug; deferred from the Tier 2 standards audit of `lib/home` because of the ripple. |

## Focused audits (future-proofing reviews)

Deep single-subsystem reviews in the style of AUDIT_STORAGE.md (2026-07-05:
storage/mount/drive — base judged solid; its 11 fixes shipped 2026-07-06; doc removed
after everything shipped, see git history). The 2026-07-01
god-file audits (mount, drive, mail, calendar) are closed and pruned. Sequencing principle:
**audit a layer right before a proposal builds on it** — findings are cheapest to fix then, and
the auditor's context transfers straight into the implementation.

| Area | Scope | Trigger / when | Effort |
|---|---|---|---|
| **Standards audit of the whole codebase (Tier 2)** | Every non-test, non-generated source file outside `packages/sheet` (~1,150 files, ~124k lines), one directory at a time in risk order: `apps/api/src/lib` domain classes, then `packages/lib/src/core` hooks, then `packages/ui` layout and shared components, then the apps (the thin ones last, paired with the frontend test harness row above). Per directory: a cold Opus agent per 15–25 files, briefed only with AGENTS.md + CODE-STANDARDS.md + the Review Standard, grades each file better / neutral / worse on the judgment rules the gates cannot express (comments that say what instead of why, single-use helpers, defensive code on trusted internals, wrong layer, duplicated facts) into a local ledger under `docs/superpowers/audits/`; then the fixes for that directory land on their own branch with `bun run check`, a cold re-audit and a small merge. Skip generated files and files under thirty lines. | One directory per session, as background work between features; never as one big bang — a repo-wide report that nobody fixes decays, and blind cleanup in areas without tests is churn with invisible regressions. The mechanical half (casts, interfaces, biome-ignores, JSDoc, deep imports, layer violations, raw colours) is already gated and ratcheted by `scripts/check-standards.ts`; lower its baselines as each directory is cleaned. | ~4–6M Opus tokens for the audits, about the same again for the fixes, spread over weeks |
| **Collab + chat containers** | `lib/collab/` — CollabDocument lifecycle, DbProvider update/snapshot persistence (SNAPSHOT_INTERVAL consolidation), `blob-codec` zstd seam, `enforceReadAccess`; chat's registry-less db lifecycle (AUDIT_STORAGE item 4 is its boundary) | **First act of the CRDT format migration (P0)** — audit the current lifecycle before rewriting its persisted format; the historical data-loss incidents surfaced in this layer | M |
| **Home lifecycle + relay** | `lib/home/` idle-destruct/touch races, async-singleton patterns, eviction predicates; `home-relay.ts` as the sharding seam | Before the relay outbox + fd-budget Phase 2 land (both modify it); prerequisite for any multi-server work | M |
| **Auth + guests + share registry** | better-auth wrapper, `verifyProtocolAuth` (one bug = IMAP + CalDAV + CardDAV + WebDAV), guest lifecycle/cleanup, `addRegistryEntry` | Before SSO adds a second identity path | M |
| **Ops path** | `backup.sh`, `update.sh`, `docker/`, first-run setup — restore drills and upgrade rehearsals, not just code reading | With DATA_INTEGRITY Phase 5 (backup.sh); the VITE-env-var update breakage shows this class recurs | S–M |
| **SSE ↔ query-key invalidation contract** | Every `sse-events.ts` emitter vs `sse-handlers.ts` + `*Keys` in `packages/lib`: does every mutation emit, does every event invalidate the right owner-scoped keys | Any time — mechanical, subagent-friendly sweep; kills the "randomly stale UI" bug class | S |
| **WebDAV/CalDAV protocol conformance** | RFC edges on the WebDAV (drive) side: LOCK/UNLOCK (Class 2) against Finder and Windows Explorer, MOVE overwrite semantics, large-file behavior | Low urgency — the CalDAV/CardDAV side was verified against real clients 2026-08 (If-Match/If-None-Match now RFC-7232-correct with tests, href encoding parity, sync-token semantics aligned with sabre's 403) and the drive side 2026-07-05; run as a verification exercise when protocol bugs surface | S–M |

Not worth re-auditing now: mount/drive/storage (just done), mail + calendar (2026-07-01, findings
closed), the sheets engine (dev-frozen format, own fidelity program), versioning (covered 2026-07-05).

**Kick-off prompt** (swap the scope line; this shape produced AUDIT_STORAGE.md):

> Read AGENTS.md and docs/CODE-STANDARDS.md. We are 1+ year and 4000+ commits into Eigen; I want
> the base to stay solid — clean, easy to read, robust, simple, performant, no over-engineering.
> Be as unopinionated as you can and do a deep dive into **\<AREA\>**. Do research: read everything
> in scope, follow paths, run the tests, and probe the real code (dev server + throwaway accounts
> allowed; `scripts/s3-local` has MinIO). Cross-check `docs/proposals/PROPOSAL_*.md` and past audits so you
> don't re-report known work. Signal over volume — "clean" is a valid verdict. Write findings and
> recommendations to `docs/AUDIT_<AREA>.md`.
