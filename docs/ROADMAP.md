# Roadmap

Prioritized backlog of the work still to do on **the road to a trustworthy 1.0**. Eigen runs in production but is honest about being pre-1.0, so the data-integrity work comes first. The P3 deferrals and everything else that waits until after 1.0 (or until a trigger appears) live in [ROADMAP-POST-1.md](ROADMAP-POST-1.md). Status is what exists today, not what the proposal hoped for; the detailed designs live in the `docs/proposals/PROPOSAL_*.md` files referenced per row.

Eigen does not implement backward compatibility for persisted formats before 1.0: a format-breaking change is done outright now (existing data is dropped, or converted with a one-off), and a migration system becomes mandatory only from 1.0. No pre-1.0 item left on this page breaks a persisted format; the remaining format breaks live in [ROADMAP-POST-1.md](ROADMAP-POST-1.md).

A grant application (NLnet Restack, call opens 2026-09-03, deadline 2026-11-03) funds the data-integrity push; the funding and launch tactics themselves are tracked separately in `docs/superpowers/strategy/` (local-only) and are not duplicated here.

The current focus, ahead of announcing the open-source repository, is the self-hoster's path: install, update, whole-server backup and a mail-off install. Its ordered work list is [SELF-HOSTERS-FIRST.md](SELF-HOSTERS-FIRST.md); the rows below stay the source for status and design.

**Legend** — Effort: S / M / L / XL (rough T-shirt size). Frozen-format: pre-1.0 the column flags a change to a persisted format (Yjs roots, DB schema, drive MIME values) that drops or converts existing data when done.

---

## P0 — Data-trust foundation for 1.0

The product's stated core weakness ("I would not yet trust it with data you cannot afford to lose") and the work the grant funds. Highest strategic value.

| Item | Status in code | Effort | Frozen-format | Notes |
|---|---|---|---|---|
| **Data integrity + verified backups** ([proposal](proposals/PROPOSAL_DATA_INTEGRITY.md)) | A corrupt snapshot fails the load with 422, and per-home backup verifies what it packs ([BACKUP.md](BACKUP.md)). Seam A exists only as a SQLite header probe when the upload queue drains (`apps/api/src/lib/mount/upload-queue.ts`); seams C/E/G, the size verify, `lib/integrity/`, the scheduled corruption sweep and phase ④ are not started. | M | Yes, low — additive `paths.integrityCheckedAt` column (sweep cursor) | Cost-tiered checks at every write seam, a scheduled corruption sweep (incl. the permanent orphaned-container scan), and alerts on what they find. Whole-server backup is its own P1 row; after it, phase ④ migrate: restore an archive on another server (identity/share re-linking) + live mount-level storage-backend migration (local↔S3) reusing copy/upload-queue machinery. Spec: [PROPOSAL_BACKUP_RESTORE.md](proposals/PROPOSAL_BACKUP_RESTORE.md). |
| **E2E multi-user collab test suite** ([proposal](proposals/PROPOSAL_E2E_COLLAB_TESTING.md)) | Not started. | M | No | Playwright, multiple concurrent clients, CI. Must be reliable from the start — flaky suites get abandoned. Phase 1: four docs/Tiptap tests on one shared convergence primitive; retries=0. |

## P1 — High value-per-effort, start next

| Item | Status in code | Effort | Frozen-format | Notes |
|---|---|---|---|---|
| **Docker-only install + Synology DSM preset** ([proposal](proposals/PROPOSAL_DOCKER_ONLY_SETUP.md)) | 0%. Today install and update both need host Bun (`bun install`, full frontend build, `update.sh`), the API image bakes `.env.production` into its layers, and the edge Caddy bind-mounts a host-built `dist/`. The app side of a mail-off install exists (`MAIL_ENABLED=0`, an authenticated SMTP relay), but `setup.ts` never offers it. | M | No | Three milestones: (1) a POSIX `sh` launcher that runs `scripts/setup.ts` inside the API image + a two-stage frontend Dockerfile (one Caddy image for both web modes) + `env_file` instead of the baked env + the "host email here?" question in setup; (2) CI-published images by digest + a checksummed install bundle, image-based Compose with a build overlay, and a release gate that installs the previous bundle and upgrades it in CI; (3) DSM as a preset = `static` profile behind DSM's reverse proxy. amd64 first (`mvance/unbound` is amd64-only). |
| **Calendar subscriptions** ([proposal](proposals/PROPOSAL_CALENDAR_IMPORT.md)) | Subscriptions are 0%, on a complete `parseIcs` foundation and a files-as-truth store. Feed refresh must NOT reuse the public event mutations (they propagate cancellations to attendees) — one bulk `applyFeedSnapshot` with `(uid, recurrenceDate)` identity; refresh on access (web range GET + CalDAV reads), no timers. | M | Yes, **low** — additive nullable `subscription` column on `calendars` | Read-only feed subscriptions, for workspace parity. The additive `subscription` column is this row's own migration on top of `calendar.db` version 2; the feed snapshot needs its own commit contract on files (N resource writes plus one index commit is not crash-atomic). SSRF decided: private feed URLs allowed by default (`calendar.allowPrivateFeedUrls`, MinIO-on-LAN precedent) behind always-on guards (scheme, userinfo, link-local/metadata block, per-redirect revalidation, size cap); hosted multi-tenant sets it false. |
| **Sheets sync: make the op log honest** ([proposal](proposals/PROPOSAL_SHEETS_YJS_WORKBOOK.md)) | 0%. Sheets keeps one JSON snapshot string plus an op log in Yjs and its own immer undo stack; every other editor keeps the document in Yjs. The three fixes below keep the two roots and the `eigensheets/2` snapshot string; the only stored addition is step 2's `state.base` id chain, which lets a client tell a compaction from a restore or an import. The full stable-row/column-id Yjs build is a format break and lives in [ROADMAP-POST-1.md](ROADMAP-POST-1.md). | L, L, M | No — no backwards compatibility for sheets, so `state.base` needs no migration (Reinder 2026-09-03) | Three in-place fixes, in order, ~3 weeks together: (1) live clients apply batches in array order: when a batch lands before already-applied unflushed batches (local or remote), roll those back with kept inverses, apply it, reapply them, so same-key conflicts and concurrent inserts match a joiner's replay; a batch applies whole or not at all, as in the replay ([step 1](proposals/PROPOSAL_SHEETS_YJS_WORKBOOK.md#what-to-do-first); operational transform ruled out: it needs a causal base id per batch, a transforming replayer and receiver-derived config shifts, and Shape 2 gives intent preservation for free); (2) server-side compaction of the op log into the snapshot, with the tab flush removed and the grid no longer remounting on a peer's snapshot (today `key={snapshotVersion}` in `apps/sheets/src/components/sheets/editor.tsx` throws away the mounted Workbook and rebuilds it on every remote snapshot); (3) undo: shift the stacks on a peer's row, column and sheet-deletion markers, never let undo or redo clobber a peer's later edit, and let a structural undo apply its own ops. |
| **SSO / enterprise login** ([proposal](proposals/PROPOSAL_SSO.md)) | 0%. Foundation stronger than assumed — the `user.create.after` hook auto-provisions org + reconciles shares, and Home/maildir/quota bootstrap is already lazy + idempotent on first `getHome()` (no waitlist coupling — no provisioning work needed); app-password protocol-auth already works for passwordless users. `@better-auth/sso` not yet installed. v1 is OIDC-only, SAML deferred. | M | Yes — additive `@better-auth/sso` provider tables in `users3.db` (auth-schema migration) | OIDC via better-auth's `sso` plugin (SAML + setup-wizard step deferred to phase 2), providers registered at runtime → admin page. **Demand-gated:** pull forward when a concrete org needs it. Key work: disable the plugin's own org-provisioning (double-`addMember` hazard against the existing hook), gate its built-in `/sso/register` endpoint, account-linking policy; SSO users use app passwords for IMAP/CalDAV/WebDAV. **Cheaper first slice, not demand-gated:** `socialProviders` for Microsoft + Google with a tenant/`hd` pin — no plugin, no provider table, no migration — covers single-domain deployments and is the prerequisite of the IMAP-backend row in P2 (it is what holds the mail OAuth tokens). Design the login page and the account-linking rule once, for both routes. |
| **Durable home-relay outbox (queue for cross-home delivery)** ([proposal](proposals/PROPOSAL_HOME_RELAY_OUTBOX.md)) | ACL fan-out is async (bounded-concurrent, per-path FIFO, drain-on-shutdown — `acl-propagation.ts`) and the ACL route takes deltas merged server-side. Remaining: the durable outbox itself (server-level `outbox.db`, per-lane FIFO + coalescing with conditional ack, global K=8 delivery budget). | M | Yes, low — new server-level `outbox.db` (additive) | Persisted delivery rows + per-recipient FIFO + retry/backoff + replay-on-boot, following the existing `UploadQueue`/`ContentReindexQueue` pattern (third instance of "durable rows + self-scheduled drain"). Closes the crash-loses-in-flight-deliveries window and replaces silent per-recipient skips with retry. Also the stated sharding trajectory — in a multi-server deployment the outbox *is* the message transport (see SCALABILITY.md). **Build it in the relay, not per seam** — the seam audit found the same sync-fan-out pattern in: (1) `propagateCalendarShare` (`calendar/share-propagation.ts`, awaited on the share PUT — direct sibling of the fixed ACL bug, N sequential cold home opens); (2) chat/comment notifications (`chat.ts` mention + activity loops, awaited inside `postMessage` — cold opens for every previous participant add latency to every message send; coalesce-by-tag makes ordering a non-issue); (3) `FileHistory.notifyWatchers` (`drive/history.ts`, concurrent but **unbounded** `Promise.all` awaited on every drive mutation — the fd-burst class); (4) signup reconciliation (`share/reconciliation.ts`, sequential in the `user.create` hook — rare, note only). Calendar invitations (`invite-propagation.ts`) are already fire-and-forget but sequential and carry a real create-vs-update ordering hazard (`sequence` can apply out of order) — they need the outbox's per-target FIFO, not just parallelism. One global concurrency budget in the relay/outbox subsumes all per-seam semaphores and closes the fd-burst class everywhere at once. |
| **File-descriptor budget: graceful exhaustion** ([proposal](proposals/PROPOSAL_FD_BUDGET.md)) | Compose pins `ulimits: nofile`. Discovered during the slow-share investigation: a 26-home fan-out **crashed with `SQLITE_IOERR_VNODE` under macOS's default `ulimit -n 256`**, silently skipping recipients. Measured warm ceiling ≈ 30 fds per open Home (6 eager WAL databases × 3 files + 12 maildir `fs.watch` handles; more per extra mount / open doc container). | S | No | Phase 1: document the `LimitNOFILE` requirement for bare-metal deploys + startup check — read the soft limit via `process.report` userLimits, log a loud warning below a derived threshold. Phase 2 (optional, gated on need): LRU cap on resident Homes via the existing `homeFactories`/`touch()` seam (eviction predicate incl. open collab connections) + `ManagedDatabase.open` retry-once-after-eviction. |
| **DAV auth costs a password hash and a session row per request** | Every `/dav` request re-authenticates from scratch, and on the primary-password path that means a full password hash plus a minted session: `verifyProtocolAuth` (`apps/api/src/lib/auth/protocol-auth.ts`) falls through to `auth.api.signInEmail`, which verifies the hash (~54 ms) and writes a `session` row. Measured: 200 device PUTs took 12.2 s through `/dav` against 0.5 s at the store, and 220 requests left 224 new `session` rows behind. Shared by CalDAV, CardDAV, WebDAV and the mail SASL path (Postfix and Dovecot through `routes/internal.ts`), since all of them sit on the same helper. An app password short-circuits earlier on a cheap key lookup, so a client set up the recommended way avoids it, but a client set up with the account password pays it on every request of every sync. | S–M | No | The fix is verifying the password **without** minting a session (better-auth has no public "check this password" entry point, so this is the part that needs designing) plus a short-lived verified-credential cache keyed on the email and a hash of the password. Both halves are security-sensitive — a cache turns a revoked password into a valid one for its TTL, and a bare verify must keep the 2FA hard-gate and the failure limiter — so it needs its own security review before it lands. |
| **Phase ③ — whole-server backup** ([proposal](proposals/PROPOSAL_BACKUP_RESTORE.md) § Phase ③) | Phase ② left every primitive generic on purpose: `snapshotHome` writes any home into a folder, `packFolder`/`extractArtifact` take any folder (the `glob?` argument exists so one home can be pulled out of a server archive), `verifyFolder` judges any folder with a manifest, the job runner is generic over a job function, and the manifest's `kind: 'server'` + `homes[]` fields are reserved. Nothing of ② needs rework. | S–M | No — the server manifest is additive; no persisted format changes | Net-new: the all-homes enumerator over `data/home`, `data/team` and `data/org`, cross-checked against the auth `user` and `team` tables (guest homes skipped, org homes are a folder copy); the `server/` folder (`users3.db`, `eigen.db` **and `waitlist.db`** via `VACUUM INTO`, plus `config.json`, `settings.json` and `avatars/`); one pack of the whole staging root; retention (keep the last N); a `scheduleInterval('server-backup', …)` entry with the schedule and retention in server settings and an admin Settings § Backups section; and the optional off-server upload to a bucket that is not the one the data lives in (revisit encryption at rest with it — decision D2 deferred it to this phase). `.env.production` sits outside `data/` and stays the operator's job, which [BACKUP.md](BACKUP.md) says out loud. Retires the offline `scripts/backup.sh`, which eigen.is still depends on (unverified, whole-server, seconds of downtime, no per-home restore), so it must not slip far behind ②. It is also where the per-home backup route's `getHome(ownerId)` moves behind home-relay (cheap-wins row). |
| **Save to Drive beside every export** | `FileSubject` and the `FILE_ACTIONS` registry drive the preview overlay and every file menu ([PREVIEWS.md](PREVIEWS.md)), but every export (docx, xlsx, pdf, vCard, `.ics`) is a direct download through `useFileDownload` (`packages/lib/src/core/download.ts`) with no Save to Drive beside it. | S | No | Needs a third builder, `subjectFromBlob(blob, name)` (the export hooks fetch a URL, so it takes the fetched response), and the export hooks running the registry's `download` / `save-to-drive` through `useFileActionRunner` instead of `useFileDownload`; the upload goes through the existing `useUploadFile`, since the bytes are already in the browser. Both contacts import routes stay: the registry's `import-contacts` reads a Drive subject server-side through `import-from-drive` and posts the bytes for a subject that has no Drive path. |

## P2 — Finish what's already started

| Item | Status in code | Effort | Frozen-format | Notes |
|---|---|---|---|---|
| **Vector drawing app: embedding (phases 5–6)** | Not started: no `drawings/` sub-resource exists, and the element model has no `groupId` (`packages/lib/src/vector/types.ts`). | M | No (`.eigenvector` shape may still change — not BC-frozen) | Drawings embedded in docs/sheets as nested `drawings/` sub-resources (phases 5–6; the slides half is superseded by the canvas engine); grouping deferred to a future round unified with slides' object model, alongside the last-used style memory row. Ruled out for good: minimap, shape library, PNG export. |
| **Vector: frames and presenting a drawing** | The engine already takes a `viewport` of `infinite` or `frame` (`packages/ui/src/components/vector/canvas-editor.tsx`) and `addFrame` exists in `use-canvas-doc.ts`, but the vector shell hardcodes `viewport="infinite"` (`apps/vector/src/components/vector/editor.tsx`) and `addFrame`'s only caller is the slides editor, so a drawing can hold no frames. `PresentMode` (`apps/slides/src/components/slides/present-mode.tsx`) is likewise imported by slides alone. | S–M | No | Frames in a drawing are how you lay out a poster, a set of boards or a storyboard, and presenting one is nearly free once they exist. Both are host-shell work on engine machinery that is already built and tested — no model change. Do them together. |
| **Canvas element model: container text, per-deck frame size, richer gradients** | Three edges of the element model. (1) No `containerId` or bound-text field exists in `packages/lib/src/vector/types.ts` or `kinds/` — only arrows own an inline label (`text`), so text over a shape is an independent element that stays put when the shape moves. (2) `FRAME_WIDTH`/`FRAME_HEIGHT` are constants (`packages/lib/src/vector/frames.ts`), deliberately never stored, so every deck is 1920×1080. (3) `FillPaint` (`packages/lib/src/types/background.ts`) is a two-stop *linear* gradient and `strokeColor` is a plain color string (`vector/types.ts`) — no radial gradients, no gradient strokes. | S–M each | Additive per item — a `containerId` field, a stored per-deck frame size, a radial variant in the fill codec; `.eigenvector`/`.eigenslides` shapes are not BC-frozen | Independent of each other, none blocked. Container text is the one people notice first (a label should follow its shape); per-deck frame size is what 4:3 and poster decks need; the gradient pair is a small widening of one codec plus its two renderers. |
| **SVG-native rich text** | Rich text is HTML everywhere: `scene-layers.ts` emits a div and `scene-to-svg.ts` wraps that div in a `foreignObject`, so any consumer of a vector SVG needs an HTML renderer (this is why the PDF path goes through the canvas compositor). No `text`/`tspan` path exists in `kinds/richtext.ts`. | M | No | Only worth building if a consumer appears that cannot render HTML — a `text`/`tspan` renderer means re-implementing line breaking, alignment and inline marks, and it would be a second source of truth for typography next to `richTextCssText`. Recorded so the trade-off is not re-litigated from scratch. |
| **Search: calendar events** ([SEARCH.md](SEARCH.md)) | Mail + drive search (names and body content) exist ([SEARCH.md](SEARCH.md)); calendar is the last unsearchable domain. | S | Yes, low — additive FTS table, following the inline-FTS pattern mail.db uses | First item of SEARCH.md § Remaining. |
| **File History: email digests + version-history unification** ([FILE-HISTORY.md](FILE-HISTORY.md)) | Core exists: `FileHistory`, Recent Activity, Watch, Watched view, in-doc Activity panel + live SSE refresh ([FILE-HISTORY.md](FILE-HISTORY.md)). | M | Yes — email channel needs a `notifications.db` migration | Remaining: email/digest channel, unifying the in-doc Activity panel with Version History, and richer sheet/slide/doc semantic events. |
| **Last-used style memory (canvas apps)** | Not started. No Eigen app remembers last-used styles; Excalidraw does. | S–M | No | If built: ONE shared store consumed by vector and slides (and stickies where sensible), never a vector-only quirk. Decide when canvas usage feedback exists. |
| **Copy-Paste Phase 0** ([proposal](proposals/PROPOSAL_COPY_PASTE.md)) | v1 clipboard live; the async path (`writeEigenClipboardAsync`, `packages/lib/src/core/clipboard/clipboard.ts`) writes `text/html` + `text/plain` only, where the sync writer also sets `application/eigen-clipboard`. | S | No (clipboard is transient) | Meant to make canvas (slides and vector) button-copy lossless, but the proposal's fix is not available as written: `navigator.clipboard.write` cannot set an arbitrary custom type, which is why the async payload rides the HTML marker span instead ([CLIPBOARD.md](CLIPBOARD.md) § Asymmetry). So the row is really a question — measure what a button copy actually loses against an event copy, and either close the gap inside the marker or record the asymmetry as intended. Full ECP v2 protocol is in [ROADMAP-POST-1.md](ROADMAP-POST-1.md). |
| **Help center: article gaps** | ~170 articles exist ([HELP-CENTER.md](HELP-CENTER.md)); shortcut references exist for Mail, Slides and Vector only. | S | No | Unwritten articles: keyboard shortcuts for Docs/Sheets/Drive; docs "import from another doc" + real-time-collab guide; admin self-hosting guide (decide where it lives). Blocked-on-feature articles (sheets charts/pivots, slides themes/speaker notes, calendar subscribe, contacts merge-on-import, chat formatting, mail filters/out-of-office/schedule-send) wait for the features. Contextual `getHelpUrl(section, slug)` deep links were never built. |
| **Frontend unit-test coverage** | The harness exists (`packages/ui/src/test/happy-dom.ts`, rendering through `react-dom/client`, no `@testing-library/react`); what is missing is coverage. | S–M | No | `packages/ui`'s state machines are uneven (`useLongPress`, the drill-in menu, `useDocumentPanels` have no tests), and nine apps have no `src/test/` at all (admin, calendar, chat, contacts, docs, drive, sheets, space, vector; the thin-apps cheap-wins row below). Pin behavior (timer thresholds, page stacks, one-panel-at-a-time), not rendering details. |
| **Mobile: next round** ([MOBILE.md](MOBILE.md) § Next round) | The sheet tab strip is hidden on phones, stickies' activity PanelColumn is desktop-only (`apps/stickies/src/components/stickies/board.tsx`), and Drive has no Select mode. | S–M | No | The item list lives in [MOBILE.md](MOBILE.md) § Next round; accepted drifts are recorded in the same file. |
| **Command palette v2** ([proposal](proposals/PROPOSAL_COMMAND_PALETTE.md)) | v1 exists (engine + 13 UI files); all six v2 items unbuilt. | M | No | Sub-actions, `event:`/`chat:` scopes, recents, paste/drop into the palette. Nothing blocked on it. |
| **Comment threads: per-user unread state** | No per-thread read state is stored: chat containers hold no user ids, so a per-user read-state table has to be added keyed by **email**. The comment badge is personal — it counts open threads assigned to you; "unread" is the signal it cannot give. | S–M | Yes, low — adding an email-keyed table and widening the comments list response | Today the only per-user signal is notifications: comment replies and mentions carry the thread in their tag (`chatActivityTag` / `chatMentionTag` in `packages/lib/src/core/notification/tags.ts`), so per-thread unread is derivable the way `useUnreadChatIds` derives per-chat unread — but coverage is partial by design (activity notifications go to previous participants + owner only, so a member who never replied has no signal). Work: add an email-keyed per-user read-state table — write it when the card dialog opens, return it on the comments list so `CommentEntry` carries `unread`, then decide what the badge and the panel rows show (unread dot per row, and whether the toolbar badge becomes assigned-to-me + unread). Preferred over the notification derivation: it survives notification purging and covers people who were never notified. |
| **Team-removal collab read-revocation** | Not started. Read-revocation is coherent for `updateACL` + `movePath`; removing a member from a *team* still doesn't revoke their live collab read access on docs in other homes. | S–M | No | Cross-home, so it needs the relay — bundle with the P1 durable home-relay outbox rather than building a bespoke fan-out. |
| **Mail that lives at an existing provider — IMAP backend** ([proposal](proposals/PROPOSAL_EXTERNAL_MAIL_PROVIDER.md)) | Seam only: `MailStore` has one implementation, and `MAIL_ENABLED=0` is the sole answer for a user whose mail lives elsewhere — no Mail app, no inbound invitations or RSVPs. | L (1,200–1,500 LOC), phased | No | An `ImapStore implements MailStore` makes the Mail app a client of the user's existing mailbox: the provider stays MX and source of truth, Eigen reads over IMAP, sends over the provider's SMTP submission and keeps only the summary cache, so read state and folders agree with the user's other clients. Auth is XOAUTH2 with the token from SSO sign-in — no stored passwords — so **the SSO row's `socialProviders` slice lands first**; password-only providers wait for a secret-storage decision. Needs no tenant admin rights and no public host; the reference case is one Microsoft 365 mailbox on a `localhost` instance. Phases: read path → send + Sent → iMIP from synced messages (configurable trusted authserv-id). Copying mail in via provider-side dual delivery was considered and rejected — a second mailbox that drifts. |
| **Mail: export a whole mailbox** ([PREVIEWS.md](PREVIEWS.md)) | One stored message downloads as an `.eml` (`GET /mail/:ownerId/message/:id/download`), but nothing exports a whole mailbox, where `.vcf` exports the whole book and `.ics` a whole calendar. | S | No | Also open: a `.eml` preview has attachments of its own that `FileSubject` (`packages/lib/src/types/file-subject.ts`) has no address form for — a part inside a previewed file. |

## Cheap wins (broken-window fixes)

| Item | Effort | Notes |
|---|---|---|
| Sheet engine Excel-parity gaps left by the 2026-09-15 audit | S–M | The engine audit fixed twenty-two Excel-semantics bugs; five findings need a decision or a grammar and are filed in [SHEETS-TODO.md](SHEETS-TODO.md): a date-typed formula result shows its serial in a mask-less cell (§ Formula engine), formulajs parses ISO date strings as UTC (§ Formula engine), range intersection (`=A1 B1`) needs a production in the hand-maintained parser tables, not a lexer rule (§ Formula engine), conditional-format value coercion differing per rule type (§ Bugs, needs Q2), and whether a typed `5/1/2026` becomes a date (§ Q13). |
| `use-collab-doc.test.ts` flakes under the full check | S | `packages/lib/src/test/core/collab/hooks/use-collab-doc.test.ts` failed once inside `bun run check` (2026-09-14) with an unhandled `window.event` TypeError from react-dom between tests and `Cannot call afterAll() after the test run has completed`, then passed alone and in the whole lib suite. A React update fires after the file's `afterAll` restored the happy-dom globals (`installHappyDom`, `packages/lib/src/test/happy-dom.ts`); the test already unmounts in `afterEach`, so the fix is to flush the pending React work before the globals go. Re-check whether it still reproduces on the current helper. |
| ACL delta `add` has no count cap | S | `PUT /drive/:ownerId/:mountId/path/:pathId/acl` bounds each `add[].id` by `MAX_EMAIL_LENGTH` but not the number of entries: 300 new addresses in one request store in ~130 ms, and with `notifications.email.guestOnAclAdd` on every account-less address gets an invite mail, so one authenticated request mails N arbitrary addresses (the sibling `access-check` route caps at `MAX_SEND_RECIPIENTS`). A `maxItems` on `add` is a product call (what is the largest legitimate share list?) — found by the routes standards audit 2026-09-14, not changed there because it alters what a client may send. |
| Client history event details are unbounded | S | `POST /drive/:ownerId/:mountId/path/:pathId/history` takes `details.card` / `toColumn` / `cardId` as bare `t.String()` (a 200 KB card text stores as one `file_events` row); only the 30 s dedupe window bounds it. Cap the strings at the schema (`clientFileEventBody` in `routes/shared-schemas.ts`) to what the activity row renders. |
| A vCard import can claim the self-link | S | `importCards` (`apps/api/src/lib/contacts/transfer.ts`) writes every card through `putCard` with `If-None-Match: *`, and `resolveSelfLinkOnPut` (`apps/api/src/lib/contacts/dav-store.ts`) hands a create the `eigenId` link when the card's `X-EIGEN-ID` or an exact owner-email match wins and no row holds it yet. So a `.vcf` a user imports can carry their own address and become their self card, with `X-EIGEN-ID` written into the stored bytes. Same rule a device PUT gets (deliberate there), but an import is a bulk action nobody inspects card by card. Either guard the importer (never claim the link on an imported card, leave it to the owner-email rematch `resolveSelfLinkOnPut` already runs) or make the claim opt-in. |
| The label fan-out rewrites cards past the storage budget | S | Every card write takes `writeCard` (`apps/api/src/lib/contacts/contacts.ts`), which holds both ceilings — `CARD_MAX_BYTES` and `enforceHomeDataQuota` — before it commits. The label fan-out does not: `rewriteCardCategories` (`apps/api/src/lib/contacts/labels.ts`) rewrites every member card's `CATEGORIES` through `indexCard` inside the rename's transaction and settles the byte delta after it, with no ceiling in front. A rewrite can only grow a card by the length of one label name, so the overshoot is small and bounded by the member count — but a Home at its budget grows past it. Either charge the accumulated delta once, before the transaction, with the edit grace the rewrite already qualifies for, or say in [QUOTA.md](QUOTA.md) that a label rename is unmetered by decision. |
| An avatar upload gets no edit grace at a full budget | S | `enforceAvatarUpload` (`apps/api/src/lib/config/enforcement.ts`) checks `used + fileSize > max` directly instead of going through `enforceHomeDataQuota`, so it credits nothing for the avatar it replaces and gets neither the rewrite exemption nor the 1 KiB grace every other data-half write has ([QUOTA.md](QUOTA.md)). A user at their budget therefore cannot replace a 2 MB photo with a 1 MB one, where the same user can still rewrite a contact card or an event. One call through `enforceHomeDataQuota(userId, fileSize, currentAvatarSize)` is the whole fix; the credit needs the staged sibling's size, which `uploadAvatar` already has. |
| An `.ics` cannot be imported into a calendar another **user** shared with you | S | `resolveTransferCalendar` (`apps/api/src/routes/calendar.ts`) resolves a team home by `checkCalendarAccess`, so a team calendar the caller may write in is a valid import target, but any other foreign `ownerId` meets `requireSelf` and answers 403 — including a calendar a user shared with `write`, because the write would run in that Home and only the relay crosses Homes. The same resolver refuses an export from such a calendar. It needs an `importEventsAt` in `apps/api/src/lib/home/home-relay.ts` beside `createEventAt`, and the route then resolving every target through `checkCalendarAccess`. Until then the picker offers own and team calendars only, which is what it does. |
| A calendar can grow past what one `.ics` imports back | M | `Calendar.exportEvents` (`apps/api/src/lib/calendar/transfer.ts`) joins every stored resource into one string in memory and nothing bounds it, while both import routes bound one FILE — `ICS_MAX_BYTES` (5 MB) and `ICS_IMPORT_MAX_EVENTS` (10 000 VEVENTs). So a calendar above either ceiling exports whole and answers 413 on the way back in (probed 2026-09-21: two 4.1 MB imports export as one 8.6 MB file). The fix is a streamed export plus an import that reads a large file in parts, not a second pair of constants. |
| Mail compose shows filename-less parts the API sidecar drops | S | The draft sidecar keeps a part only when it has a filename (`mail-domain.ts`), while `initFields` (`apps/mail/src/components/mail/hooks/use-draft.ts`) chips every part and names the unnamed ones through `mailAttachmentName`, so a filename-less part shows as a compose chip the server never counted — the keep list can then never match `meta.attachments` and every save of that draft takes the slow path (`keepAll` false). One rule on both sides, the way `isIcsMime` (`packages/lib/src/types/drive.ts`) is the one rule for what iCalendar is. |
| Every range request on a mail part re-parses the whole message | S | `readMailPart` (`apps/api/src/lib/mail/serve-mail-part.ts`) answers a matching `If-None-Match` off the summary row, but any miss calls `messageGetAttachment`, which re-reads the `.eml` and runs the full MIME parse — a seeked video pays one parse per range. A small per-home LRU of parsed messages, keyed by message id + the summary row's size and date (the same inputs as the ETag), would bound it. |
| Space and Enter no-op after a select-all with no cursor | S | `packages/ui/src/hooks/use-keyboard-list-navigation.ts` still guards quick look (Space) and open (Enter) with `selectedIndex >= 0`, so ⌘A on a fresh list selects every row and neither key does anything — the same cursor-less selection the Delete fall-back now covers. Delete had one obvious answer (act on the selection); opening or previewing the first row of a 200-row selection is a product call. |
| xlsx export drops floating images | S | The HTML/PDF sheets export renders `Sheet.images` (`renderFloatingImages`, `apps/api/src/lib/export/sheets/render.ts`), but the ExcelJS writer path (`to-xlsx.ts`) emits nothing for them, so the one export format meant to round-trip loses every floating image. ExcelJS has `worksheet.addImage` with the same cell anchors the renderer already resolves. |
| **Migrate to Bun 1.4.x** (pinned to 1.3.14 in `.bun-version`, CI and `docker/api/Dockerfile`) | S | Bun 1.4.2 balloons the API test process from ~1.2 GB past the memory limit late in the suite (GitHub kills it with exit 143; in Docker with a 7 GB cgroup it dies 3/3 on main, every time inside `export-pdf-ssrf.test.ts`'s data: image test, right after the hyperlink test; never under 1.3.14). Not a test: single files and a 40k-call sanitizer loop stay flat, and hoisting the test's regex into a variable made one run pass, so it is JIT-shaped and Bun-internal. **Retest on every new Bun release** (they ship every week or two): `git ls-files -z \| rsync -a0 --files-from=- . /tmp/eigen-linux/`, then `docker run --rm --memory 7g -e GITHUB_ACTIONS=true -v /tmp/eigen-linux:/work -w /work oven/bun:<ver> sh -c 'bun install --frozen-lockfile && cd apps/api && for i in 1 2 3; do bun test --preload ./src/test/preload.ts; done'`; ~15 minutes. Gate: three runs complete (exit 0 or 1 with only the two CardDAV mtime failures a bind mount causes) with no `Killed`. Then bump `.bun-version`, CI and the Dockerfile in one commit, and the host on eigen.is with the README one-liner. If two consecutive releases fail the gate, build the minimal upstream repro from the death site above and file it with Bun. Bun 1.4 also ships `bun dedupe`; a dependency round had to dedupe stale nested lock entries by hand (delete the nested `"x/pkg"` lines from `bun.lock`, wipe every `node_modules`, reinstall). |
| Phase ③ job runner: move the backup home lookup behind home-relay | S | `POST /admin/backup/home/:ownerId` (`apps/api/src/routes/backup.ts`) calls `getHome(ownerId)` for another user's home and hands it to the backup job — allowed (the guard permits `getHome` in routes, as `delete-user.ts` does) and correct while every home lives on one server. Phase ③ runs the job on the server that owns the home, so the lookup becomes a relay call next to `pullHomeSize`; the job runner and the routes are the only places that change. |
| IMAP keeps writing the old home while a restore replaces it | S | A per-home restore (`apps/api/src/lib/backup/restore.ts`) refuses the home on every surface that resolves it per request — HTTP, SSE, collab WS, CalDAV, CardDAV, WebDAV all meet `ApiError(503, 'Restore in progress')` from `getHome`. IMAP does not: Dovecot runs in its own container over the shared volume (`mail_location = maildir:~/Maildir`) and only calls the API to check a password, so a client connected while the restore runs keeps reading and writing the Maildir inside the folder that was moved aside. Deliveries and flag changes made in that window are in `{id}.pre-restore-{ts}`, not in the restored home. Bounded (the length of one restore) and nothing is lost, so it is documented as a known window ([BACKUP.md](BACKUP.md) § What the user experiences during a restore) rather than fixed in code. A real fix means stopping the Dovecot container for the window, or a per-user lock it honors. |
| Backup upload stops at 1 GB; a bigger archive goes in by scp | S–M | `POST /admin/backup/artifacts` streams the raw body into a temp file and refuses a `Content-Length` over `BACKUP_UPLOAD_MAX_BYTES` (1 GB, `packages/lib/src/constants/backup.ts`, which the API's own `maxRequestBodySize` derives from). A larger archive has to be copied into the backups folder by hand and then verified from the pane ([BACKUP.md](BACKUP.md) § Bringing an archive from another machine) — fine for an operator on the box, useless for anyone restoring a home onto a server they only have the browser for. Phase ④ work: a chunked, resumable upload (fixed-size parts into `.staging/`, assembled and manifest-checked on the last one), which is the same machinery a cross-server import wants. The artifact format itself does not change. |
| No GC for bucket objects orphaned by a deleted home | S–M | Deleting a user removes the home folder outright (`teardownUserData`, `apps/api/src/lib/user/delete-user.ts`) and never touches an `s3` mount's objects, so they stay in the bucket with nothing left pointing at them. Pre-existing, and per-home backup adds a second route to the same end: `deleteSafetyCopy` (`apps/api/src/lib/backup/safety-copy.ts`) normally deletes the objects only that copy references, but with the live home already gone it has nothing to compare against, logs that it is leaving the objects alone, and removes the folder anyway. The restore side of this is done and deliberate — a remote restore gives every restored row a fresh storage key, so no object is ever overwritten and a safety copy stays complete — what is missing is a sweep that lists a mount's bucket prefix and deletes keys no `metadata.db` on this server references. Wants a deliberate design before code: the same sweep would happily delete the bucket an admin is re-pointing a mount at, so it needs a dry run, an age floor, and an explicit answer for a mount whose `s3Config` is gone. |
| Boot recovery parks a pristine safety copy as `.failed-restore-` | S | `restoreSafetyCopy` (`apps/api/src/lib/backup/restore.ts`) installs by renaming the chosen `.pre-restore-` copy onto the home folder, then runs `checkRestoredDatabases`, then writes the completion note. A process killed inside that window leaves the pristine copy at the home path without the note, and `recoverInterruptedRestores` (`recovery.ts`) cannot tell it from a half-written extraction: it parks it as `{id}.failed-restore-{ts}` (Delete only in the pane) and puts the newer pre-restore copy back. Nothing is deleted and the home is whole, so it is not data loss, but restore.ts says out loud that a `.failed-restore-` name on a pristine home is what the in-process rollback avoids. Fix: the `restoring.json` marker carries the name the folder should be parked under (the copy's own name for a safety-copy restore, `.failed-restore-` for an archive), recovery uses it, and a boot pin covers the window. Found by the backup-restore standards audit (2026-09-13). |
| A folder named like a standard mailbox in another case is invisible | S | `canonicalMailbox` (`packages/lib/src/constants/mailboxes.ts`) case-folds the six standard names at every domain entry, so on a case-sensitive file system an `.archive` an IMAP client made beside `.Archive` is skipped by the enumeration (`isStandardMailbox` claims the name) and unaddressable (every spelling of it resolves to `Archive`): its messages are reachable over IMAP only. What it takes: stop case-folding the standard names in `canonicalMailbox` — keep only `INBOX` → `''`, which IMAP itself defines as case-insensitive — and map the lowercase route segment back on the frontend, where `mailboxRouteSegment` is what lowercased it. macOS is case-insensitive by default, so this bites on Linux hosts. |
| A standard folder recreated by an IMAP client loses its file watcher | S | `MaildirStore.watch()` attaches `fs.watch` to `cur/` and `new/` of the standard six once, when the home loads (`maildir-store.ts`), and the listing kicks a background reconcile only for folders outside those six. Deleting and recreating one of them from an IMAP client leaves the watcher on the old directory, so nothing filed into the new one surfaces in real time until the home reloads — it does reconcile when the folder is opened, which is what keeps it from being data loss. Fix: re-attach the watcher on its `error`/`close`, or watch the Maildir root for the recreate. |
| Every mail SSE event refetches the mailbox list | S | Each of `MAIL_RECEIVED`, `MAIL_MOVED`, `MAIL_DELETED`, `MAIL_READ_CHANGED`, `MAIL_DRAFT_UPDATED` and `MAIL_SENT` calls `invalidateMailboxes` (`packages/lib/src/core/mail/sse-handlers.ts`), and the store broadcasts one `received` per message, so a 250-message sync chunk is 250 refetches of `GET /mail/:ownerId/mailboxes` — each of which re-lists every folder on disk. Pre-existing. A debounce on the counts invalidation (the list refetch is already suppressed for an own echo) is the whole fix; the message-list invalidation stays immediate. |
| A draft whose only part is an `.ics` sends as single-part `text/calendar` | XS | With no subject text, no body and one calendar attachment, `createEmlContent` (`apps/api/src/lib/mail/mailfile.ts`) produces a message whose top-level headers are the part's — `Content-Type: text/calendar; name=invite.ics`, `Content-Disposition: attachment`, base64 — not `multipart/mixed` (probed 2026-09-21). Legal, and it reads correctly in the clients checked, but a receiving MUA is then free to treat the whole message as an invitation rather than a mail with one attached. Recorded rather than changed (2026-09-21): forcing `multipart/mixed` for a one-part draft changes the bytes of every such send. |
| A preview format bump does not reach a browser that cached a Drive preview | S | The vCard, `.ics` and text Drive preview routes answer with a long `max-age` and their URL carries only `updatedAt`, so a fix to a payload builder or a sanitizer is invisible until the file itself changes — the `*_FORMAT` tags (`apps/api/src/lib/preview/preview-cache.ts`) only invalidate the server's own `previewsDir` cache. The mail part-preview routes and the Drive `.eml` preview route already avoid it by carrying the format tag in the ETag under `private, no-cache`. Fix: put the format tag in the Drive preview URL (or its ETag) so a bump revalidates. |
| An `.eml` quick look drops a whole stylesheet over one fetching rule | S | `sanitizeEmlHtml`'s `uponSanitizeElement` hook (`apps/api/src/lib/preview/eml-preview.ts`) empties a `<style>` element whenever any rule in it fetches, so a newsletter whose sheet holds both `.hero { background: url(https://…) }` and `.wide { width: 900px }` reaches the quick look with `<style></style>`: the table loses its declared width, and `ShadowContent`'s `overflow-wrap: anywhere` then lets it collapse to a character or two per cell on a 375 px viewport. The mail reader, which sanitizes without that hook, keeps the sheet and scales the 900 px table down to fit. Measured 2026-09-21: injecting the missing rule into the quick look's shadow root reproduces the reader exactly (container 900 px, host `scale(0.34)`), so the quick-look host and `ShadowContent` are not the cause. Fix: drop only the rules that fetch instead of the whole sheet, re-checking the filtered text with `cssFetches` and falling back to emptying it — a security-boundary change, which is why it was not taken with the frontend pass. |
| Contacts edit form: external sync discards unsaved edits (product call) | XS–S | The edit route remounts the form when the contact's etag changes (`key={(contact || emptyContact).etag}`, `apps/contacts/src/routes/_auth.edit.$filterType.$filterId.tsx`), so a CardDAV sync landing mid-edit silently resets the form to the synced card. Accepted during the CardDAV final review; the call is between keeping it (form always reflects fresh data) and an isDirty guard + conflict prompt before remounting. |
| DAV consistency wave: deferred minors | S | Triaged fine-to-defer and unlikely to ever justify a dedicated session: WebDAV PROPFIND (`lib/webdav/propfind.ts`) still answers a fixed prop set regardless of the requested props — the RFC 4918 §9.1 prop-list handling covers the CalDAV/CardDAV surfaces only; reuse `lib/dav/propfind.ts` if a client ever cares (deliberate scope cut, WebDAV is litmus-tested against Finder/Windows Explorer). |
| CalDAV serves the whole resource where a client asked for part of one | S | `calendar-data` is answered with the stored bytes whatever the request narrowed it to: a `<C:comp>`/`<C:prop>` subset, an `<C:expand>` of a recurring series or a `<C:limit-recurrence-set>` window are all parsed past and ignored (`apps/api/src/lib/caldav/xml-parser.ts` reads only the filter, `report.ts` serves `getResource` verbatim), where the CardDAV twin honors its own partial retrieval through `projectAddressData` (`apps/api/src/lib/carddav/address-data.ts`). Harmless for correctness — a superset of properties is still the resource — but a client that asked for a narrow projection pays the full bytes, and `expand` is the one a phone uses to avoid expanding recurrence itself. The shape to copy is the CardDAV one: a projection function beside the builder, driven by what the parser reads out of `<C:calendar-data>`. |
| `calendar-query` ignores `prop-filter`, `param-filter` and `text-match` | S | The parser (`apps/api/src/lib/caldav/xml-parser.ts`) reads the component names and the VEVENT `time-range` and nothing else, so every event in that component and window is answered whatever the filter narrowed it to — a client looking one event up by UID (python-caldav's `event_by_uid`, Evolution's every query) gets the whole range back and filters it itself. A superset is the safe answer, since these are mandatory grammar (RFC 4791 § 9.7) and refusing them refuses the collection, but it costs the bytes of a collection per lookup. First step: evaluate a `UID` `text-match` from the index, which is one indexed column and covers the shape clients actually send; a general evaluator means matching against the stored component, next to the `calendar-data` projection the row above wants. |
| A DAV REPORT buffers its whole multistatus before it answers | S | Both `handleReport` (`apps/api/src/lib/caldav/report.ts`) and `handleCardReport` (`carddav/report.ts`) push every row into a `string[]` and hand the joined result to `multistatusResponse` (`dav/xml.ts`), which builds one more string and passes it to `new Response`. So an initial sync of a large collection holds the whole answer in memory at once — up to `REPORT_DATA_BUDGET_BYTES` (32 MiB) of resource bodies plus the XML around them, and more than once over while the copies are joined. The budget keeps it bounded, which is why this is a cost rather than a risk. The fix is to stream: a `ReadableStream` that emits the envelope, then one row at a time as each resource is read, then the closing element and (for `sync-collection`) the token. Both surfaces take the same change, because the row builder (`dav/report-row.ts`) is already shared and already per-row. |
| The CalDAV and CardDAV sync-collection handlers are still twins | XS–S | `lib/dav/` holds everything else the two protocol layers share (envelope, propfind, `report-row.ts`, `sync-token.ts`, `href.ts` with `resolveMultigetHrefs`, `write-result.ts`), and both `resource.ts` files are thin adapters over it. Still written twice: the `handleSyncCollection` body in `apps/api/src/lib/caldav/report.ts` and `carddav/report.ts` (initial listing, token check, the `getChanged*Since` + `getDeleted*Since` delta, token append), and the per-href lookup loop after `resolveMultigetHrefs`. The store seams are the same shape, so the extraction into `lib/dav/` is mechanical. The protocol tests (`test/caldav/caldav.test.ts`, `test/carddav/carddav.test.ts`, both on `davRequest` from `test/dav-test-helpers.ts`) are the behavior pin. |
| The `Calendar` class is a pass-through facade | S | `apps/api/src/lib/calendar/calendar.ts` holds the state that must be one per Home — the write lock, the database handle, the byte counter, the broadcast batch — and the commit seams over it, and then some fifty public methods, most of which are a single line forwarding to a plain function in `dav-store.ts`, `events.ts`, `shares.ts`, `invitations.ts`, `occurrences.ts` or `transfer.ts` that takes the facade straight back as its first argument. The split itself is right (the modules are testable, the cycle is broken by the type-only import), but the forwarding layer is a file to keep in sync for no behavior of its own. Either let the routes and the DAV layer call the sibling functions with the facade — which is what the siblings already do to each other — or keep the forwards and say in [CALENDAR.md](CALENDAR.md) that one entry point is the point. Not worth a dedicated round; decide on the next real touch of the domain. |
| `sync-collection` ignores `<D:limit><D:nresults>` | XS | A client that caps how many changes it wants per round (RFC 6578 § 3.7) gets the whole delta anyway: `parseReport` (`apps/api/src/lib/caldav/xml-parser.ts`) does not read the element and `handleSyncCollection` (`report.ts`) has nothing to bound the loop with. The byte budget keeps one response finite, so this is a politeness gap rather than a risk; honoring it means truncating the delta AND answering the token of the last row served, never the collection's current one. The CardDAV twin has the same gap. |
| Guests of a CalDAV-created event never get the first invitation | S | A CalDAV PUT writes the event with its `ATTENDEE` lines but fans nothing out: `handlePut` (`apps/api/src/lib/caldav/resource.ts`) goes to `Calendar.putResource`, which is the store seam and has no invitation path at all, where the REST mutations gate `propagateInvitation` on the acting `user`. So a meeting Apple Calendar or Thunderbird created reaches its guests only if the owner later touches it in the web app, and then the mail says "Updated invitation" for something they never received; deleting it says "Canceled". Pinned as current behavior by `caldav-roundtrip.test.ts` ("a CalDAV PUT mails the guests nothing"). The product call is which end moves: send the initial `REQUEST` from the first web edit when no attendee has ever been mailed, or fan out from the PUT itself (which needs the acting `User`, and makes every client sync a potential mail send). |
| An invitation carries the projected event, never the stored component | S | Both invitation transports send projected fields — `propagateInvitation` (`apps/api/src/lib/calendar/invite-propagation.ts`) builds its relay payload from the row, and the iMIP body is serialized from a `CalendarEvent` — so anything an organizer's `.ics` holds that Eigen does not model never reaches a guest's copy. Harmless today: a CalDAV PUT fans nothing out (the row above), so no event with guests can carry unmodeled content in the first place. If that row is ever resolved the other way, a PUT becomes the first source of one, and that is when carrying the stored component across a Home boundary — stripped of `X-EIGEN-*` at exactly one place — becomes worth building. Until then projecting is what keeps local stamps out of an external organizer's mailbox by construction. |
| An iMIP invitation to a series carries the master alone | S | `serializeEventForImip` (`apps/api/src/lib/ical/ical-component.ts`) builds one VEVENT, so `composeInviteEmail` mails an external guest the series master without the override VEVENTs and the master's `EXDATE`s beside it — the guest's client renders a moved occurrence at its original slot and renders one the organizer deleted. RFC 5546 wants one VCALENDAR holding the master and its overrides; `buildResource` already assembles exactly that shape from a master plus its exception rows, so the fix is to let the iMIP body take the same list and keep `stripEigenStamps` over it. An Eigen guest gets the exceptions as occurrence messages instead (docs/CALENDAR.md § Invitations). |
| A collaborator inviting the calendar owner never delivers | S | `propagateInvitation` (`invite-propagation.ts`) skips only the acting user's own address, so a write-share holder who adds the calendar's owner as a guest relays an invitation into that owner's Home — where `decideInboundRequest` (`calendar/invitations.ts`) finds the owner's own stored master under that UID, sees that it names no organizer matching the verified sender, and drops the message with "the stored event names another organizer". The drop is clean and logged rather than a constraint failure, but the owner is still never invited to the event in their own calendar. Fix is a product call: treat the owner as already attending (skip the relay and mark the attendee accepted), or let the adopt rule accept a sender who holds `write` on the calendar the event lives in. |
| A team calendar locks an event one of its members organized | S | `isInvitationFromOthers` answers "is this someone else's invitation?" by address, and a `TeamHome`'s synthetic user has an empty one (`getSyntheticTeamUser`, `team-home.ts`), so any stored `ORGANIZER` on a team calendar reads as foreign. A member who creates an event with guests on the team calendar from Apple Calendar or Thunderbird therefore writes a row nobody can edit afterwards: later PUTs are accepted and discarded, the web app disables the fields, and deleting it declines instead of canceling. Needs a rule for what a team's "own address" is — the organizing member's address is on the row, but treating any member as the team is a permission decision, not a lookup. |
| An event organized from an alias address stays locked | S | The same address rule, from the other side: `isInvitationFromOthers` (`packages/lib/src/core/calendar/calendar-utils.ts`) compares the stored `ORGANIZER` with the Home owner's one address, and **no alias list exists anywhere in Eigen**, so an event a user organized from an alias — created in a CalDAV client, or an invitation addressed to the alias — reads as someone else's: the web app disables the fields, later PUTs are accepted and discarded, and deleting it declines instead of canceling. Sibling of the team row above; both want one answer to "which addresses are mine", which is a product decision before it is a lookup. |
| A write-share holder sees an owner-organized event as read-only | S | Both calendar dialogs (`event-detail-dialog.tsx`, `edit-event-dialog.tsx`) pass `email: undefined` to `isInvitationFromOthers` when the event belongs to another Home, so every event carrying an `ORGANIZER` in a shared calendar renders locked — including the owner's own CalDAV-created meetings, which the server would let a `write` holder edit. The address is not in reach: `SharedCalendar` (`packages/lib/src/types/calendar.ts`) carries `ownerUserId`, names and colors but no address, and `useSharedCalendarLabel` resolves public users for team owner ids only. `usePublicUser(ownerUserId)` / `getPublicInfo` does return it (`PublicUser.email`, `''` for a team), so the fix is ~3 lines per dialog — at the price of one more cached, batched request per calendar owner, which is why it wasn't taken with the organizer-lock fix. |
| A redelivered REPLY reverts the attendee's status on the organizer's copy | S | Every REPLY `processInboundImip` (`apps/api/src/lib/calendar/imip.ts`) verifies is applied as it comes, through `calendar.receiveAttendeeStatus` or `receiveRsvpForOccurrence` (`apps/api/src/lib/calendar/invitations.ts`): the RFC 5546 ordering guard `isNewerRevision` (`apps/api/src/lib/ical/ical-component.ts`) orders the organizer's own messages only, and a REPLY carries no revision of its own. So an accept that arrives behind the decline that replaced it — a greylisted mail, a forwarded copy — marks the attendee accepted again, silently. iCalendar has no per-attendee revision property, so ordering replies needs a stamp Eigen writes on the `ATTENDEE` line itself and compares the way it already compares the organizer's.
| Calendar's REST update takes no `expectedEtag` where contacts' does | S | `PUT /calendar/:ownerId/calendars/:calId/events/:id` (`apps/api/src/routes/calendar.ts` → `updateEventAt` in `apps/api/src/lib/home/home-relay.ts`) carries no optimistic-lock token, while the contacts twin passes `body.etag` into `Contacts.updateContact` (`apps/api/src/routes/contacts.ts`) and answers 412 on a mismatch. Two clients editing one event therefore last-write-win silently. An HTTP save is a patch against a resource a CalDAV client may have replaced ([CALENDAR.md](CALENDAR.md) § The write path), so a lost update is a live outcome rather than a theoretical one. Either add the optional etag and the 412, or write down why calendar differs. |
| `parseIcs` is superlinear on a file naming a TZID it does not define | S | ical.js caches a `getTimeZoneByID` hit, never a miss (`component.js`), so every VEVENT naming a TZID with no VTIMEZONE rescans every component of the file: measured 2 000 events = 0.09 s, 10 000 = 1.3 s, 26 805 = 21 s on one thread. The import no longer pays it (it refuses a file on its `BEGIN:VEVENT` count first) and a stored resource is one series, so the hot paths are bounded — but two passes still meet whole files: the preview builder parses whatever fits in `ICS_MAX_BYTES` inside the transform Worker, where 21 s is most of the deadline, and `rebuildProjection` parses every stored resource of a Home in one pass. The clean fix is to remember the misses, which today means reaching into ical.js's private `_timezoneCache`, so it waits for an upstream fix or a decision to reach in. |
| Mail list shows a message unread after opening it from a notification | S | Clicking a mail notification before the mailbox was ever loaded opens the message (auto mark-read succeeds on the server), but going back to the inbox inside the app shows the row unread until the one-minute stale time. Common on the demo. Known ingredients: `beginOptimisticMailMutation` (`use-emails.ts`) cancels the inbox's in-flight first fetch and restarts it in `onMutate`, which runs before the PUT, so that page can be pre-write; `patchEmailInLists` is a no-op on a list with no data; and the SSE `mail:read_changed` echo is suppressed as already patched, so nothing corrects it. A first attempt moving the restart to `onSettled` passed a hook unit test and a full-reload browser probe, but still failed the real flow (in-app navigation back to the list, same cache), so the cause is not fully pinned. Reproduce in a browser with in-app navigation and a network log before changing code. |
| `pack-verify.test.ts` times out under full-suite load | S | `apps/api/src/test/backup/pack-verify.test.ts`: a shifting four of its tests hit the 5 s default timeout in two consecutive full runs (~0.4 s each alone). Re-run in isolation before treating it as a regression; the fix is a load-tolerant timeout for the file's heavy tests. |
| rspamd sidecar + spam learn-loop ([proposal](proposals/PROPOSAL_RSPAMD.md)) | 1–2 d | The real fix for the spam/DMARC pain that the Stalwart proposal exists to solve. rspamd+Redis as a Postfix milter (edge scoring, SPF/DKIM/DMARC); `mailboxDeliver` routes flagged mail to Junk; the existing "Report Spam" button trains Bayes via `/learnspam`·`/learnham` in `messageMove`. Fail-open, no frozen-format impact. |
| Per-SASL-user outbound rate limit | S–M | The only fix that would have bounded the 17k run, since the login itself was legitimate. Postfix has no per-SASL-user message limit, so it needs a `check_policy_service`: either a small policy endpoint in the API (raw TCP, new surface) or a fourth mail container such as postfwd. Deferred as materially more work than the co-located mail-hardening fixes. |
| Audit-branch: deferred minors | S | (1) `TeamHome.updateMount` persists-before-push — a mid-push `addMount` init failure on a storage re-point leaves settings enabled with no live mount until eviction (rare, self-heals on reload; add catch-and-revert). (2) O(N·depth) descendant re-walk on permanent delete of large trees: the non-`storageKey` branch of `Mount.deletePath` (`apps/api/src/lib/mount/mount.ts`) recurses per child and invalidates sizes each time. (3) Large-numeric `@page` values in PDF export — weasyprint cost blow-up (`export/sheets/render.ts` sizes the page from an unbounded content size; `export/canvas/transform.ts` likewise). |
| Audit P3 duplication folding | S–M | Open: the copy-mutation quartet (`useCopyFiles`, `useCopyToMediaFolder`, `useCopyPath`, `useDuplicatePath` in `packages/lib/src/core/drive/hooks/writes.ts`) and the parallel per-domain action lists — the same action reimplemented across desktop toolbar, compact/kebab, right-click, command palette, and mobile menus, drifted in permission, label, keyboard, and visibility: extract a small per-domain action catalog domain-by-domain as each domain's drift is fixed, following the shape `FILE_ACTIONS` (`packages/lib/src/core/file-actions.ts`) already gives the file-ish actions, not a generic menu framework. |
| Frontend-quality-review: deferred minors | S | (1) invite read path — `messageGet` re-parses every `text/calendar` attachment with ical.js on every open (sender-controlled N×size amplification, LOW: linear, no recurrence expansion, malformed input contained); persist the summary at delivery or first-part short-circuit + size guard — needs a product call on multi-invite messages. (2) `export/sheets/render.ts` spells the sans stack two ways in one file (hardcoded vs `FONT_STACK_SANS`); unifying shifts golden-pinned export bytes, wait for a golden-regenerating touch. (3) `TEAM/SPACE_SETTINGS_UPDATED` SSE members exist but nothing emits them (dead handlers were deleted) — either emit on the settings routes for cross-device freshness or drop the members. (4) eigendoc list/shared views hand-roll `useDriveListRoute`'s contract — the list view has quick look; unifying adds preview-on-select there and both to the shared view, a product call. (5) Drive's detail-kebab labels doc export "Download" (intentional, reads ambiguously next to true downloads). (6) Avatar uploads lost byte-level progress when the workflow moved onto a lib fetch mutation (accepted; restoring means moving the XHR transport). (7) `apps/api/package.json` exports `./src/index.js` for a `.ts` file (pre-existing, type-erased importers only). |
| Mail-attachment-access: deferred minors | S | Beyond send-time access grants (MAIL.md § Send-time access grants), unbuilt and not asked for since: a server setting to force a policy (always grant / never prompt), pasted-URL detection, an "anyone with the link" option, the passive attach-time "Only you can open this" pill, and one-click retry for failed recipients. Then the review leftovers: (1) `buildRecipientSummary` (`mailutils.ts`) doesn't flatten RFC 2822 groups for the list row / search text — a product call, since flattening `undisclosed-recipients:;` blanks the To column where the group name is the only label. (2) Attachments-only send (no subject, no body, one file) is blocked FE + BE — consistent, but as legitimate as the ref-only send now allowed. (3) `handleReplyEmail` (`apps/mail/src/components/mail/hooks/use-mail-actions.ts`) passes the original `Reply-To` AddressObject through the `text` path, so a group there round-trips as parser text — left because `text` preserves quoting for names with commas. (4) The compose-side flow (`sendWithFreshDraft`, `ShareAndSendDialog`, the save lifecycle) has no test harness; three of the six must-fix items lived there. |
| Batch idempotency: duplicate-on-retry in client/loop batches | S–M | Client-driven and sequential-loop batches aren't retry-safe: a partial failure leaves the succeeded items persisted, and a retry from the (correctly retained) picker/dialog re-sends them, duplicating. Instances: drive copy (`useCopyFiles` sequential loop, `packages/lib/src/core/drive/hooks/writes.ts`) and mail attachment save (`saveAttachmentsToDrive`, `apps/api/src/lib/mail/mail.ts`, a sequential write loop). The window is narrowed (validate-before-write on save-to-drive) but the general class wants a per-item partial-result contract or a server-owned batch op. Server-side sibling is the P1 home-relay-outbox fan-out class. |
| WebDAV per-user resource bounds | S | Two tables grow without a cap per authenticated user, the noisy-user class of the chunked-PUT gap in [WEBDAV.md](WEBDAV.md): dead properties on `DrivePath.details.webdavProps` are capped at 64 KB per PROPPATCH body (`MAX_XML_BODY_BYTES`, `webdav/webdav-router.ts`) but not across requests (`webdav/proppatch.ts`; every PROPFIND of the parent then carries them), and shared LOCKs on one path accumulate per user until the 24 h TTL (`drive/lock-manager.ts` has no per-path or per-user count). A cap per path with 507 / 423 is the fix; the numbers are a product call. |
| Docs desktop comment panel overlays the page scrollbar | S | The docs right-side panels (comments, activity, and the figure and table properties panels that share the slot) are an absolute `inset-y-0 right-0` overlay by design, so opening one never shifts the layout; below `PANEL_CLEAR_WIDTH_PX` the page tucks under the panel — it slides left by its overlap with the text column and only scales once that slack runs out (`apps/docs/src/components/docs/editor.tsx`). What the overlay costs is the scroll container's own scrollbar, which it covers. A fix has to keep both: inset the scrollbar (`scrollbar-gutter`) or pad the scroll container while a panel is open, without moving the page. Laying the pane out as a flex sibling the way sheets and slides do was tried and rejected on 2026-09-15 — the scroll box then ends at the pane's edge, but the page shifts, which is exactly what the shift-and-scale math exists to prevent. |
| Sheets: arithmetic overflow yields `Infinity` instead of `#NUM!` | S | `=2^10000` and `=1E308*10` hand `Infinity` to the cell, where Excel reports `#NUM!`. One guard on the numeric operators in the formula engine, which already throw `#VALUE!` on a NaN result ([SHEETS-TODO.md](SHEETS-TODO.md) § Formula engine). |
| Thin frontend apps: low comment density, zero tests | S–M | admin, calendar, drive, chat and contacts sit at roughly 1–3.5% comment lines and have no tests (the other apps are 5–11%). Thin apps need few comments, but these are also where the per-domain action lists have drifted (see the Audit P3 duplication row) — read them for duplication first, and make them the first target of the P2 frontend unit-test coverage row. |
| Bun-native micro-touches | S | `createHash('md5')` and `createHash('sha256')` → `Bun.CryptoHasher` in `s3-storage.ts`, the last `node:crypto` hashes (the DAV etags are `Bun.CryptoHasher` already, in `core/blob-store.ts`); `Bun.spawn({ timeout, killSignal })` over the hand-rolled `setTimeout → kill` in `export/weasyprint.ts` + `shared/video-thumbnail.ts` (native reaping, matching the exiftool timeout fix). Marginal — do on next touch of those files. |
| Canvas: three files past the split line | S | `packages/lib/src/vector/geometry.ts` (991 lines, several exports existing only for tests), `packages/ui/src/components/vector/tools/use-drawing-tools.ts` (1,095) and `packages/ui/src/components/vector/canvas-editor.tsx` (1,628, still holding the pointer-gesture handlers) each carry several concerns at once. Split along the seams they already have — outline/hit-test/binding maths, per-tool drawing state, gestures — on the next real touch of each, not as a dedicated round. |
| Admin `team-detail.tsx` is four panes in one file | S | 470 lines holding the team header + settings form, the mounts list with its two `MountDialog`s, the backup section and the members list, with a dozen drafts and mutations in one component. Split along the seams it already has — a settings form, a mounts panel, a members panel — the way `backup-section.tsx` is its own file. |
| Shared drive watch list is N+1 | S | `SharedDrive.getWatches` (`apps/api/src/lib/drive/sharedDrive.ts`) calls `breadCrumb` once per watched path to re-check read access, so a user with many watches on a shared drive pays one ancestor walk per row on every list. Batch the ancestor lookup per mount, or cache the crumb per parent id within the call. |
| `Home` subsystem getters are typed non-null but absent on team, guest and org homes | S–M | `home.contacts`, `home.mail`, `home.calendar` and `home.notifications` return non-null types via definite-assignment fields, yet `TeamHome` builds only drive + calendar, `GuestHome` drive + notifications and `OrgHome` nothing, so callers optional-chain through non-nullable getters (`calendar/shares.ts`, `calendar/invitations.ts`, `mail-domain.ts`, `shared-with-me.ts`, `Home.dataSize`, `home-relay.ts`) and the `drive` getter hides a `!` over its now-optional field. `hasCalendar` is the one flag that exists, and `share/reconciliation.ts` asks it instead of optional-chaining — the pattern to finish. Honest fix: a `has*` flag per subsystem (or `X \| undefined` getters) and a sweep of the consumers. Typing debt, not a runtime bug; deferred from the Tier 2 standards audit of `lib/home` because of the ripple. |

## Focused audits (future-proofing reviews)

Deep single-subsystem reviews. Sequencing principle: **audit a layer right before a proposal builds on it** — findings are cheapest to fix then, and the auditor's context transfers straight into the implementation.

| Area | Scope | Trigger / when | Effort |
|---|---|---|---|
| **Standards audit of the whole codebase (Tier 2)** | Every non-test, non-generated source file not yet audited, one unit per session. Remaining, in value order: (1) auth/storage/config/scheduler/setup/user batched in one session, judged on absences (what bounds this, who validates that) rather than cast count; (2) the remaining routes (`chat.ts`, `space.ts`, `editor.ts`, `team.ts`, `settings.ts`, …) folded into their domain's session rather than as one routes unit — the drive/collab round showed a route file audits well only with its domain class and hooks open; (3) chat paired with its lib hooks, LIGHT: one cold auditor plus the orchestrator's lead pass, not three batches — internal and well tested; (4) calendar together with CalDAV (`calendar.ts`, `lib/ical`, `lib/caldav`, `lib/core/blob-store.ts`), taken as one unit with `routes/calendar.ts`, the lib calendar hooks, share-propagation, invite-propagation, recurrence and iMIP verification (the two DAV servers mirror each other, so audit each pair side by side to catch divergence); (5) notification-center, versioning, share, search, waitlist as one small batch; then `packages/lib` beyond the hooks (its own row below), `packages/ui`, then the apps (thin ones last, paired with the frontend test coverage row above). **Audit each API domain together with its `packages/lib` hooks and its `types/<domain>.ts` in the same session**: the type chain runs handler → Eden → hook → component, and an auditor who sees one end cannot tell a seam cast from a missing return type at the other. Per unit: a cold Opus agent per ~2,000 lines, briefed only with AGENTS.md + CODE-STANDARDS.md + the Review Standard, grades each file better / neutral / worse on the judgment rules the gates cannot express (comments that say what instead of why, single-use helpers, defensive code on trusted internals, wrong layer, duplicated facts; each cast judged seam vs laziness with the narrowing that removes it) into a local ledger; the fixes land on their own branch with the goldens as the behavior pin, `bun run check`, a cold Fable re-audit that traces every hunk through untouched callers, then a small merge. For a unit that meets an external protocol or on-disk bytes, the orchestrator writes a throwaway probe test for its leads (what does a malformed stamp, header, path or body do, before which side effect) BEFORE the auditor reports arrive and rules from its output: in past units the probes found the seam bugs the auditors' rule list missed. Probe every mutation route against the rule its sibling already enforces (create vs rename, ACL id vs chat invite email, copy dedup vs upload dedup): a half-fixed class hides in exactly that gap, and an auditor reading one route never sees it. Skip generated files and files under thirty lines. Yield note: well-tested internal code surfaces drift rather than bugs; the remaining bugs sit where external protocols meet our data — mail, the DAV servers, iMIP — so weight the sessions there and do not feel obliged to run every thin frontend app through the full machinery. A forked engine with Excel as its spec is the same kind of seam: auditors find its bugs by executing the engine on their claims, not by grading from the rule list. Two cleanup hazards the re-audits keep catching: moving a raw class string into `cn()` lets tailwind-merge resolve a contradiction the stylesheet order used to decide (diff the rendered tokens per site), and "put the shared helper next to its owner" produces ESM cycles (brief the import direction, not only the home). | One unit per session, as background work between features; never as one big bang — a repo-wide report that nobody fixes decays, and blind cleanup in areas without tests is churn with invisible regressions. The mechanical half (casts, interfaces, biome-ignores, JSDoc, deep imports, layer violations, raw colors) is gated and ratcheted by `scripts/check-standards.ts`, whose live numbers sit in `scripts/standards-baseline.json`; lower its baselines as each unit is cleaned. | ~0.7–1.2M Opus tokens per unit (three auditors, four implementers, three Fable re-audits, one fix round), spread over weeks |
| **`packages/lib` beyond the gates** | The mechanical numbers are already low there (core hooks 10 casts, `sheets` 9, `vector` 4, `docs` 6), so the Tier 2 pass through it should look for absences, not style: every owner-scoped query key carries `ownerId`, every mutation routes errors through `onMutationError`, every SSE handler invalidates the keys its emitter changes (see the SSE ↔ query-key row above) — judged per domain, paired with that domain's API session. Two concrete items: the sheets op log (mostly in `packages/sheet`, with its snapshot codec and Yjs op helpers in `packages/lib/src/sheets`) needs the three in-place fixes of the P1 sheets-sync row before any bigger sheets move; `types/drive.ts` is where one-fact-two-places drift collects (it holds a private twin of `getExtension` that cannot import the shared one from `constants/preview` without a cycle) and wants a split into per-concern modules rather than an audit. | With the paired domain sessions above; the op-log fixes are the P1 sheets-sync row | Folds into the domain sessions; the op-log fixes are sized in the P1 row |
| **Collab + chat containers** | `lib/collab/` — CollabDocument lifecycle, update/snapshot persistence (`SNAPSHOT_INTERVAL` in `collabDocument.ts` against `writesPerSnapshot` in the collab and chat `db-config.ts`), `blob-codec` zstd seam, `enforceReadAccess`; chat's registry-less db lifecycle | **First act of the CRDT format migration** ([ROADMAP-POST-1.md](ROADMAP-POST-1.md)) — audit the current lifecycle before rewriting its persisted format; the historical data-loss incidents surfaced in this layer | M |
| **Home lifecycle + relay** | `lib/home/` idle-destruct/touch races, async-singleton patterns, eviction predicates; `home-relay.ts` as the sharding seam | Before the relay outbox + fd-budget Phase 2 land (both modify it); prerequisite for any multi-server work | M |
| **Auth + guests + share registry** | better-auth wrapper, `verifyProtocolAuth` (one bug = IMAP + CalDAV + CardDAV + WebDAV), guest lifecycle/cleanup, `addRegistryEntry` | Before SSO adds a second identity path | M |
| **Ops path** | `backup.sh`, `update.sh`, `docker/`, first-run setup — restore drills and upgrade rehearsals, not just code reading | With [SELF-HOSTERS-FIRST.md](SELF-HOSTERS-FIRST.md) blocks 1–3, whose restore drill and release gate are this audit's live half; the VITE-env-var update breakage shows this class recurs | S–M |
| **SSE ↔ query-key invalidation contract** | Every `sse-events.ts` emitter vs `sse-handlers.ts` + `*Keys` in `packages/lib`: does every mutation emit, does every event invalidate the right owner-scoped keys | Any time — mechanical, subagent-friendly sweep; kills the "randomly stale UI" bug class | S |
| **WebDAV/CalDAV protocol conformance** | RFC edges on the WebDAV (drive) side, the measured ones from the 2026-09-14 standards audit first: (1) `webdav/propfind.ts` validates the body and then answers allprop to every request — no prop selection, no 404 propstat, `Brief` ignored — while `lib/dav/propfind.ts` (`parsePropfind`/`selectProps`/`wantsBrief`) is the shared machine for exactly this; riding on it means adopting the dav `multistatus`/`response`/`propstat` builders and `encodePathSegment` too (WebDAV hrefs return `%40` for `@`, which a client may treat as a different resource), so every multistatus changes and it needs a client round (Finder, Explorer, rclone, Mountain Duck); (2) DELETE, MOVE, COPY and PROPPATCH evaluate only the `If` lock header and silently ignore `If-Match`/`If-None-Match` (RFC 7232 §3.1: MUST 412); (3) `LockManager.isWriteAllowed` demands every covering lock be the writer's own, so two shared-lock holders can never write (RFC 4918 §7.5); (4) a LOCK refresh (empty body + `If`) does not check the token's owner; (5) `app.ts` advertises `DAV: 1, 2, 3, calendar-access, addressbook` on `/dav` with an `Allow` listing `MOVE, COPY, LOCK, UNLOCK`, none of which that router implements (trimming is a behavior change clients see first). Older: LOCK/UNLOCK (Class 2) against Finder and Windows Explorer, MOVE overwrite semantics, large-file behavior | Low urgency — the CalDAV/CardDAV side is verified against real clients (If-Match/If-None-Match RFC-7232-correct with tests, href encoding parity, sync-token semantics aligned with sabre's 403) and the drive side too; run as a verification exercise when protocol bugs surface | S–M |

**Kick-off prompt** (swap the scope line):

> Read AGENTS.md and docs/CODE-STANDARDS.md. We are 1+ year and 4000+ commits into Eigen; I want the base to stay solid — clean, easy to read, robust, simple, performant, no over-engineering. Be as unopinionated as you can and do a deep dive into **\<AREA\>**. Do research: read everything in scope, follow paths, run the tests, and probe the real code (dev server + throwaway accounts allowed; `scripts/s3-local` has MinIO). Cross-check `docs/proposals/PROPOSAL_*.md` and past audits so you don't re-report known work. Signal over volume — "clean" is a valid verdict. Write findings and recommendations to `docs/AUDIT_<AREA>.md`.
